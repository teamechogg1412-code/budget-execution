# UI 개편 구현계획서

- 목적: 계산 엔진은 그대로 두고, 화면만 **읽히는 대시보드**로 다시 짠다.
- 작성: 2026-08-21
- 상태: 합의용 초안 (구현 전)

---

## 0. 한 줄

숫자는 지금과 같게 두고, **헤더·간격·위계·표의 숨 쉴 공간**만 손본다.  
다크 테마·카드 폭격·차트 위주 대시보드는 하지 않는다.

---

## 1. 왜 지금 지저분해 보이는가

지금 화면은 기능은 많은데 **한 화면에 도구막대가 너무 많다.**

| 현상 | 원인 |
|---|---|
| 본문이 늦게 시작됨 | 상단이 sticky. 제목 + 저장/가져오기 + KPI 4칸 + 탭까지 겹쳐 `--setup-sticky-top: 310px` |
| 종이 조각이 반복됨 | `.card`가 테두리·여백 같은 박스를 화면마다 반복 |
| 입력 화면이 엑셀처럼 보임 | 수익·비용이 표 + 체크 + 작은 글씨 안내가 한 덩어리 |
| 분석은 이미 표가 본체 | 월별 원장은 대시보드화하면 오히려 못 읽음 |

개편의 핵심은 **상단을 얇게**, **대시보드만 한눈에**, **입력/분석은 밀도 유지**다.

---

## 2. 안 바꾸는 것

- 계산 엔진, 세율, 인센티브, 식대 `(식대+50%)×2`, 종소세 연도 분할
- 베이지·아이보리 바탕, 잉크색 글자, 포인트는 딥그린(`#1f5c4d`)
- 서버 없는 로컬 HTML. 프레임워크 추가 없음
- 탭 구성(대시보드 / 설정 / 수익 / 비용 / 분석 / 설정)의 **하는 일**
- 원 단위 숫자. 화면에서만 만 원·억 병기

---

## 3. 목표 느낌

참고 이미지는 Linear·Stripe 같은 **여백 있는 업무 화면**이지, Notion 갤러리나 모바일 월렛이 아니다.

- 배경 `#efe8d8`, 패널 `#fbf7ee`, 선 `#e6dcc9`보다 옅게
- 제목 하나, 숫자 하나, 설명이 짧다
- 표는 그대로 두되 행 높이·숫자 정렬·헤더 고정만 다듬는다
- 그림자는 쓰지 않는다. 테두리만

---

## 4. 화면별 방향

### 4.1 공통 셸 (가장 효과 큼)

지금: 제목 / JSON·엑셀 버튼 / KPI 4칸 / 탭이 한 덩어리로 항상 붙음.

바꿀 것:

1. **1줄 헤더** — 배우명 · 기간 · 저장 상태만. 가져오기·시드 되돌리기는 `⋯` 메뉴
2. **탭은 그 아래 한 줄** — 지금 nav와 같되 패딩만 정리
3. **KPI는 대시보드에만 크게.** 다른 탭은 기말·최저·기간입금 3개만 작은 줄
4. sticky 높이를 310px → **약 88~120px**로

이 한 단계만 해도 “대시보드 같다”.

### 4.2 대시보드

지금: 안내 문단 + 사업 규모 표 + 자금 안전성 + 차트 + 사업 성과 표가 세로로 길다.

목표: **결론이 위에, 표는 아래에.**

```
[ 기말 현금          ] [ 월말 자금(세후)     ] [ 1인 vs 전속 차이 ]
[ 최저 잔액 · 월     ] [ 추가 필요자금       ] [ 기간 입금        ]

좌: 월별 잔액 스파크라인 (기존 SVG 차트 정리)
우: 올해 / 내년 세전·세후 4~6행

그 아래: 사업 규모 · 성과 표 (연도 열 유지)
맨 아래: 경고만 접힌 목록
```

기간말 현금 / 월말 자금 / 청산용 잔여는 **라벨을 카드에 명시**한다. 숫자를 합치지 않는다.

### 4.3 수익 · 비용 (입력)

표 구조를 없애지 않는다. 대신:

- 목록은 **한 줄 요약** (이름 · 금액 · 기간 · 진행비 · 상태)
- 수정은 지금처럼 펼치기. 펼친 본문만 섹션 제목(지급 / 진행비 / 수수료) 간격을 넓힌다
- 오른쪽 「등록 수익」은 스티키 요약 패널 유지. TOTAL 3줄은 더 크게
- 촬영기간 옆에 **이 건 진행비 n원 · 월별 반영 n개월**을 한 줄로 (진행비 오해 재발 방지)

### 4.4 분석

월별 원장·시나리오 비교·종소세·매출하한은 **표가 본체**다.

- 카드로 쪼개지 않는다
- 왼쪽 구분 열 고정, 월 헤더 고정, 숫자 `tabular-nums`
- 소계·영업이익·월말 자금 행만 배경을 한 단계 진하게
- 서브탭(시나리오 / 하한 / 종소세 / 월별) 스타일만 셸과 맞춤

### 4.5 시뮬레이션 설정 · 설정

2차 탭은 유지. 필드 그리드만 4열→ 라벨 위 입력, 섹션 사이 여백.

---

## 5. 구현 순서 (엔진 손대지 않음)

| 단계 | 범위 | 완료 기준 |
|---|---|---|
| A | 토큰·헤더·탭·KPI 줄 | sticky가 본문을 가리지 않음. 베이지 유지 |
| B | 대시보드 레이아웃 | 기말/월말/경제가치 라벨이 보임. 숫자는 지금과 동일 |
| C | 수익 목록·진행비 한 줄 요약 | 언니 왕비 같은 건에서 진행비 금액이 목록에 보임 |
| D | 비용·설정 입력 밀도 | 기능 손실 없이 섹션 구분만 |
| E | 분석 표 타이포 | 가로 스크롤·행 의미 유지 |
| F | 시나리오·종소세 카드 | 기존 T열·청산 체인 유지 |

테스트: 기존 `node test/node-run.js` HTML 문자열 검사는 클래스명이 바뀌면 깨질 수 있다. **표시 문구 테스트는 단계마다 고치고, 엔진 숫자 테스트는 건드리지 않는다.**

---

## 6. 하지 말 것

- Tailwind, React, 차트 라이브러리
- 그라데이션, 박스 섀도, 이모지 아이콘
- KPI를 차트만 남기고 원 단위를 숨기기
- 월별 분석을 모바일 카드 리스트로 바꾸기
- 색으로만 구분 (경고는 문구 + 색)

---

## 7. 합의

1. **상단 KPI — 결정.** 대시보드는 크게, 수익·비용·분석·설정 등 다른 탭은 **기말 현금 · 최저 잔액 · 기간 입금** 3칸만 작은 한 줄. 숫자 확인하려고 대시보드로 돌아가지 않아도 된다.
2. **JSON/엑셀 버튼 — 결정.** 헤더는 배우명·기간·저장 상태만. JSON 내보내기·엑셀 검산·JSON 가져오기·시드로 되돌리기는 **⋯ 메뉴** 안으로. 시드 되돌리기는 메뉴 안에서도 위험 동작으로 둔다.
3. **적용 범위 — 결정.** 지금은 **셸 + 대시보드만.** 수익·비용·분석·설정은 나중에 같은 톤으로 맞춘다. 다른 탭에도 기말·최저·기간입금 미니 줄은 이번 셸에 같이 넣는다.

A(헤더·⋯ 메뉴·미니 KPI)와 B(대시보드 레이아웃)부터 구현한다.
