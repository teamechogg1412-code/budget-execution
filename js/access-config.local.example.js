(function () {
  // 로컬 전용 예시. 이 파일을 access-config.js로 복사하거나
  // index.html에서 access-config.local.js를 추가로 로드한다 (gitignore 권장).
  window.AppAccessConfig = {
    password: "",
    publicMode: false
  };
})();
