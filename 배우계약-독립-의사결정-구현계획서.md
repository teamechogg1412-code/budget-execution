# 배우 계약·독립 의사결정 플랫폼 — 구현계획서

- 작성일: 2026-08-23
- 개정: 2026-08-23 (착수 전 보완 — 보안 선행·계산 계약·진단 수치·계측·테스트)
- 상태: Phase A 착수 승인. **보안 게이트 완결 → 계산 계약·테스트 → UI** 순으로 진행
- 전제: 루트 `index.html` 비공개 정밀 도구는 유지. 공개 유입은 `/landing` + `/simulator`만.
- 배포: 정적 파일 + Supabase `campaign_links`(ref·24h) + (선택) 검토 요청 mailto/폼

---

## 0. 제품 포지션 (확정)

| 하지 않는 말 | 하는 말 |
|---|---|
| 메리디안 영입용 계산기 | 엔터 재무·정산 실무 기반 **독립 시뮬레이션 도구** |
| 우리 회사로 오세요 (첫 화면) | 내 계약·독립을 **숫자로 비교**해보세요 |
| 써니스 5% / 메리디안 15% | 법인 운영·관리비 / 매니지먼트·프로젝트 진행비 |

**한 줄:** 매니지먼트 회사가 사람을 찾아다니는 구조가 아니라, **배우가 계약·독립을 고민할 때 제일 먼저 여는 입구**.

Meridian은 퍼널 **맨 끝**에서만 등장한다.  
고객은 전속 영입만이 아니라 ①매니지먼트 ②운영대행 ③PR ④재무·정산 ⑤설립 컨설팅 ⑥커리어 등 **여러 상품군**으로 분기할 수 있다.

---

## 0.1 Phase A 선행조건 — 보안 게이트 (Gate 0)

§8의 “fail-closed·토큰 비공개”는 **완료조건**이지 과거 코드의 자동 보장문이 아니다.  
아래가 닫히기 전에는 랜딩 카피·contract UI를 외부에 올리지 않는다.

| # | 위험 | 완료조건 |
|---|---|---|
| S1 | `ref` 제거 시 `no_ref`로 통과 | `enabled:true`면 ref 누락 → `unavailable` → expired. 로컬 미리보기만 `enabled:false` |
| S2 | 임의 `ref` DB 자동 생성·승인 | 미등록 ref는 insert 금지, `unavailable`만 |
| S3 | 잘못된 `first_opened_at` → NaN 비교 통과 | `!isFinite(openedAt)` → `unavailable` |
| S4 | 게이트 비동기 완료 전 UI 노출 | `.page[hidden]` + `gate-pending` 후 통과 시에만 reveal |
| S5 | 클라이언트 비밀번호 커밋 | `access-config.js` 기본 `password:""` (fail-closed). 배포 주입만. 예: `access-config.local.example.js` |
| S6 | `visit-ping`이 봇 토큰·chatId로 Telegram URL 생성 | `notifyUrl` 프록시만. 토큰·chatId 브라우저 금지 |
| S7 | `clientIntegrity` 누락 시 SDK 로드 | 승인 CDN URL + SRI 해시 필수, 없으면 로드 거부 |

**Gate 0 체크:** `link-gate` / `visit-ping` / `access-config` / `telegram` 공개 경로에 비밀값 없음 + 위 S1–S7 회귀 테스트 통과.

> 코드 측 Gate 0 대부분 반영됨(2026-08-23). 운영 시 `LandingCampaignConfig.enabled=true`·DB 사전 등록·비밀번호·notifyUrl 배포 주입은 **배포 체크리스트**로 남긴다.

---

## 1. 퍼널 전체상

```
문자 / 검색 / 배우·매니저 공유
        │
        ▼
/landing/index.html          ← 신뢰 히어로 (실무 경력) + CTA
        │
        ├─[30초 독립 가능성 테스트]──► /landing/pulse.html     (Phase B)
        │                                    │
        ▼                                    ▼
/landing/contract.html       ← 「내 전속계약 분석」 (매출+계약 한 화면)  ★핵심
        │
        ▼
결과 = 동일 지표 2×2 + 차이 + 진단 문장 + 민감도(재계산)
        │
        ├─[정밀 비교]──► /landing/quick.html 또는 /simulator/   (기존 Quick·상세)
        ├─[계산 검토받기]──► /landing/contact.html?intent=review
        └─[계산기만 공유]──► 카카오/링크 (입력값 제외)
        │
        ▼
/landing/reveal.html         ← 질문 → (뒤에서) Meridian 소개
        │
        ▼
미팅 / 상품군 분기
```

기존 Phase 1 자산(`link-gate`, `visit-ping`, `expired`, `about`)은 재사용한다.  
`quick-engine`은 **보조 CTA·레거시**로 두고, Phase A 핵심 계산은 **`contract-engine` → 상세 엔진 프리셋**으로 새로 둔다 (§3.2).

---

## 2. 첫 화면 위계 (카피 확정안)

`/landing/index.html` 히어로 — **브랜드보다 신뢰 근거 우선**.  
(구현 시점: Gate 0·contract-engine·테스트 이후. 카피부터 바꾸지 않음.)

```
1인 기획사 설립·운영 시뮬레이션

작품·광고 수익부터 인건비, 차량, 진행비, 법인세와 개인세금까지.
현재 전속계약과 1인 기획사 운영 구조를 직접 비교해보세요.

[배지] 엔터테인먼트 업계 재무·정산 실무 경험을 바탕으로 설계
      실제 매니지먼트 비용 구조 · 배우 정산 · 법인 운영 경험을 반영

[내 전속계약부터 분석하기]     ← 주 CTA (contract.html)
[빠르게 독립 구조 비교하기]   ← 보조 (quick.html)
누가 만들었나요?               ← 팝업/about (임수연 이름은 여기만)

회원가입 없음 · 상담 없이 이용 가능 · 입력 금액은 서버에 저장하지 않음
```

- 경력 문구는 **사실관계에 맞게** 확정본만 사용  
  (`재무팀장 20년` 단정 금지 시 → `업계 재무·정산 실무 경험을 바탕으로 설계`)
- 푸터 `Developed by Meridian Collective`는 제거하거나 아주 작게. Meridian은 reveal/contact 말미.

### 2.1 `누가 만들었나요?` 팝업 (about와 동일 톤)

1. 실무에서 시작한 시뮬레이터 설명  
2. 단순 수수료 차감이 아니라 인건비·차량·진행비·법인세·대표 급여·개인세 비교  
3. 세금은 시뮬레이션 예상치 · 세무 전문가 검토 필요  
4. 그다음에야 `설계·개발: 임수연 / 운영: Meridian Collective`

---

## 3. 「내 전속계약 분석」— 수익+설정 재조합 (Phase A 핵심)

### 3.1 왜 필요한가

내부 도구는 탭이 분리되어 있다.

| 내부 UI | 배우가 인식하는 것 |
|---|---|
| 수익 탭 | 내 매출 |
| 시뮬레이션 설정 | 내 계약·부담 규칙 |
| 분석 탭 | 결과 |

공개 입구는 **「내 매출 + 내 계약조건 → 결과」 한 화면**이어야 한다.

### 3.2 `contract-engine` 아키텍처 (확정)

**`quick-engine`을 확장하지 않는다.** 별도 래퍼로 두고, 내부는 **상세 엔진(`App.Engine`)의 제한된 입력 프리셋**만 돌린다.

#### 왜 Quick 확장이 위험한가 (현행 한계)

| 한계 | 영향 |
|---|---|
| `currentAgencyCoveredCost`를 결과 계산에 미사용 | 회사 부담 비용 입력이 UX만 되고 숫자 왜곡 |
| 고액에도 단순경비율 29% 고정 | 고소득 구간 신뢰도 붕괴 |
| 법인 유보금 전체를 통제 경제가치처럼 취급 | 실수령·통제가치 개념 혼선 |
| 배당·외부 금융소득·다년도 미취급 | 상세 엔진·공개 입구 결과 불일치 |

Quick은 Phase A에서 **보조 CTA**로만 유지하고, 지표 라벨만 실수령/통제가치로 정리한다.  
신뢰 경로의 숫자는 전부 `contract-engine` 경유.

#### 입력 → 프리셋 매핑 (계약)

`contract-engine`은 공개 입력 7칸을 `App.Defaults`/`runSimulation`/`runScenarioComparison`이 받아들일 **1년·최소 시드 state**로 변환한다.

| 필드 | 상세 엔진 매핑 (요지) | 비고 |
|---|---|---|
| 연간 작품 수익 | 확정 프로젝트 1건 + 결제 | |
| 연간 광고·기타 | 영업 수익 또는 별도 프로젝트 | |
| 배우 정산 비율 | exclusive `actorShareRate` | 0~100 |
| 회사 부담 비용(연) | exclusive 회사부담 / 비용 상쇄 | **반드시 결과식에 반영** |
| 배우 부담 비용(연) | 전속=`exclusivePayer:actor`, 독립=`soloPayer:company` (법인 운영비) | 독립 후에도 필요한 비용. `soloPayer:actor` 금지(엔진에서 0) |
| 광고 별도 수수료 | revenueFees 또는 exclusive 광고율 | |
| (선택) 외부 금융소득 | `settings.tax.otherFinancialIncome` | 종합과세 판정용, 실수령 총수령에는 미포함(상세와 동일) |

**프리셋 고정 가정 (문서화·UI 고지):**

- 비교 기간: **1년** (다년은 Phase B)
- 독립 시나리오: 대표 급여 프리셋 + **배당 기본 off** (옵션으로 경계 테스트만)
- 경비율: 상세 엔진 `resolvedProfitShareExpenseRate` 경로 사용 (고액 시 단순 29% 고정 금지)
- 통제 경제가치: 상세 `controlledEconomicValue` 정의와 동일 (유보 전액 ≠ 실수령)

```text
contract.html 입력
      │
      ▼
contract-engine.buildState(input)   ← 검증·클램프·프리셋
      │
      ▼
App.Engine.runSimulation + runScenarioComparison
      │
      ▼
contract-engine.present(result)     ← 지표 추출·diagnosis·민감도
```

공개 API 스케치:

```javascript
App.ContractEngine = {
  // 입력 검증·클램프 후 내부 state
  buildState(input) -> state,
  // 시뮬레이션 + 시나리오 비교
  calculate(input) -> {
    exclusive: { actorNetIncome, controlledEconomicValue, ... },
    soloAgency: { actorNetIncome, controlledEconomicValue, ... },
    deltas: {
      actorNetIncome,           // solo - exclusive (동일 지표)
      controlledEconomicValue
    },
    diagnosis: { ... },
    sensitivity: [ { key, label, rank, deltaIfChanged } ]  // 재계산 정렬
  },
  diagnose(presented) -> diagnosis,
  sensitivity(input, base) -> drivers  // 가정 1개씩 ± 재실행 후 |Δ| 큰 순
};
```

### 3.3 페이지

`/landing/contract.html` + `landing/js/contract-engine.js`

**입력 (7칸 전후, 모바일 세로 스택)** — §3.2 표와 동일.

**출력 (개념 혼선 방지 — 확정)**

동급이 아닌 지표를 한 줄에서 빼지 않는다. **양쪽 모두 같은 두 지표**를 보여 주고, **동일 지표끼리만** 차이를 낸다.

| | 전속 | 1인 기획사 | 차이 (독립 − 전속) |
|---|---|---|---|
| 세후 개인 실수령 | A₁ | B₁ | Δ₁ |
| 통제 경제가치 | A₂ | B₂ | Δ₂ |

- 큰 숫자 영역: 위 2×2 + Δ 두 줄 (또는 탭/토글로 “실수령 기준 / 통제가치 기준”)
- **금지:** `전속 세후 실수령` vs `1인 기획사 통제 경제가치`를 단일 비교 행으로 배치
- 진단·민감도·CTA는 §4 / §5

### 3.4 지표 명칭 (상세 엔진과 정렬)

| 쓸 이름 | 의미 |
|---|---|
| 세후 개인 실수령 | 개인 통장 귀속 (`actorNetIncome`) |
| 배우 통제 경제가치 | 실수령 + 법인 세후 잔여 통제분 − 배당 조정 + 개인귀속 혜택 근사 (`controlledEconomicValue`) |

Quick·Contract·상세 화면 라벨 동일. 유보금 전액을 실수령처럼 쓰지 않는다.

### 3.5 수수료·시드 중립화

- `/simulator`의 `public-seed.js`: 수수료 표시명  
  `법인 운영·관리비`, `매니지먼트·프로젝트 진행비` (회사명 금지)
- `defaults.js` 기본 주입(`써니스`/`메리디안`)은 **공개 시드가 revenueFees를 채워 덮어쓰는 방식** 유지

---

## 4. 결과 = ‘진단’ 레이어

`contract-engine` 결과에 `diagnosis` 객체 추가. **판정 임계값은 구현 전 수치로 고정**한다.

### 4.1 판정 기준 (확정안)

비교 축은 UI에서 고른 주 지표 하나(기본: **세후 개인 실수령** Δ₁).  
상대 차이:

\[
r = \frac{\Delta}{\max(|\text{전속값}|,\, |\text{독립값}|,\, 1)}
\]

| verdict | 조건 |
|---|---|
| `similar` | \|r\| ≤ **0.05** (±5%) |
| `lean_solo` | r > 0.05 |
| `lean_exclusive` | r < −0.05 |

- 절대액이 매우 작을 때(예: 양쪽 \|값\| &lt; 100만)는 `similar`로 폴백하거나 “금액 규모가 작아 방향만으로는 판단하기 어렵다” 헤드라인 사용 (테스트로 고정).
- 통제 경제가치 축으로도 동일 규칙의 보조 판정 가능. **헤드라인 주 판정은 실수령 축** (배우가 체감하는 지표).

### 4.2 진단 객체

```javascript
{
  verdict: "lean_solo" | "lean_exclusive" | "similar",
  primaryMetric: "actorNetIncome",
  relativeDiff: 0.12,
  headline: "…",  // verdict 템플릿 + 수치 삽입 (하드코딩 “항상 solo” 금지)
  drivers: [
    { key: "actorShareRate", label: "정산 비율", rank: 1, note: "…" }
  ],
  disclaimer: "시뮬레이션 해석이며 법률·세무 자문이 아닙니다."
}
```

### 4.3 민감도 (“가장 민감한 가정”)

문구를 고정하지 않는다. `sensitivity()`가 입력 키를 **하나씩 ±재계산**한 뒤 `|Δ 실수령|` (또는 주 지표) 큰 순으로 `drivers`를 채운다.

재계산 후보 예: 정산 비율 ±5%p, 회사 부담 ±20%, 작품 수익 ±10%.

---

## 5. CTA 톤: 상담이 아니라 검토

| 나쁜 CTA | 쓸 CTA |
|---|---|
| 상담 신청 | 이 계산이 맞는지 검토받기 |
| 영입 문의 | 결과 검토 요청 (연락 방법만) |

`contact.html`

- `intent=review | general | manager`
- 필드: 이름(선택), 연락 방법, 한 줄 메모, (자동) “Contract 요약 텍스트만 붙여넣기 가능 — 서버 미저장”
- 제출: mailto 또는 기존 문의 경로. **입력 원표는 서버 금지**

---

## 6. Phase 분할

### Phase A — Gate 0 이후 (계산 계약 → UI → 카피)

목표: 공개 링크를 “영입 영업장”이 아니라 “계약 비교 도구”로 보이게.  
**순서: 보안 완결 → 엔진 계약·테스트 → UI → 진단·공유 → 히어로/Meridian.**

- [ ] **Gate 0** 보안 완료조건 S1–S7 + 배포 주입 체크 (§0.1)
- [ ] `contract-engine` 입력·출력 계약 + `test/contract-cases.js` (§12)
- [ ] `contract-engine` 구현 (상세 엔진 프리셋 래퍼)
- [ ] `landing/contract.html` 한 화면 UI (2×2 지표 + Δ)
- [ ] 진단·민감도(재계산)·검토받기·공유(금액 제외)
- [ ] Quick 결과 라벨만 실수령/통제가치로 정리 (계산 로직 확장 금지)
- [ ] `public-seed`·화면 수수료 라벨 중립화
- [ ] `landing/index.html` 히어로·이중 CTA·누가 만들었나요
- [ ] `about.html` 정렬 · reveal 말미 Meridian CTA만
- [ ] 전체 회귀 + 모바일 검증

**Phase A에서 안 함:** 30초 테스트, 재계약 전용, SEO 글, 매니저 전용 랜딩, 카카오 SDK, 금액 포함 퍼널 DB.

### Phase B — 유입 확장

- [ ] `/landing/pulse.html` — 30초 「독립 가능성 테스트」
- [ ] 「재계약 계산기」 최소판
- [ ] 매니저 진입 문구 · 공유 카드 카피

### Phase C — 검색·상품 분기

- [ ] SEO 랜딩 → 계산기 CTA
- [ ] contact intent별 상품군 분류
- [ ] (선택) 결과 PDF 클라이언트 생성만

---

## 7. 30초 「독립 가능성 테스트」 (Phase B 상세)

문항 예 (5~7개): 연 매출 구간, 정산 비율, 회사 부담, 스태프, 운영 의향.  
점수는 `pulse-engine.js`에 두고 **contract 수치와 동일하다고 주장하지 않음**.

---

## 8. 보안·개인정보 (완료조건)

| 항목 | 정책 |
|---|---|
| 금액 | 서버 저장 금지. `localStorage`는 링크 유효 기간 임시만 |
| ref | Gate 0 S1–S4. 운영 `enabled:true` + 사전 등록 행만 |
| 방문 알림 | `ref` + 페이지명 + 시각. 기기·상세 URL·금액 없음 |
| 비밀값 | access 비밀번호·Telegram 토큰·chatId 공개 경로 금지. `notifyUrl`만 |
| SDK | 승인 URL + SRI 필수 |
| about 고지 | “링크 열림 여부만 기록. 입력 금액은 서버에 저장하지 않음.” |

§0.1 Gate 0이 Phase A의 **하드 선행조건**이다.

---

## 9. 기존 문서와의 관계

| 문서 | 역할 |
|---|---|
| `배우영입-랜딩-구현계획서.md` | 폴더·link-gate·quick·simulator 복제 등 **인프라 Phase 1** |
| **본 문서** | 포지션·퍼널·contract-engine·진단·보안 Gate 0·Phase A/B/C |

인프라 체크리스트는 기존 문서 §10.  
본 문서 Phase A는 **계산 정의·테스트·보안 완료**를 카피보다 앞에 둔다.

---

## 10. Phase A 구현 순서 (확정)

1. **보안 게이트 완결** (Gate 0 / §0.1) — 미완이면 공개 배포 금지  
2. **`contract-engine` 입력·출력 계약** + §12 테스트 골격(실패하는 테스트 먼저)  
3. **계산 엔진 구현** (상세 프리셋 래퍼, Quick 비확장)  
4. **`contract.html` 한 화면 UI** (2×2 지표 + 동일지표 Δ)  
5. **진단·민감도·검토·공유** (공유 URL에 입력값 미포함)  
6. **랜딩 히어로·Meridian 노출 순서** (index / about / reveal)  
7. **전체 회귀·모바일 검증**

완료 기준: ref 링크로 들어와 **상담 유도 없이** “내 계약 vs 독립”을 **동일 지표끼리** 비교하고, 검토 요청으로 리드만 남길 수 있을 것.

---

## 11. 성공 지표와 계측 (충돌 해소)

### 11.1 기본 계측 (현행)

`campaign_links.first_opened_at` — **링크 오픈만**.  
이것으로는 “결과 완주”, “검토받기 클릭”을 **측정할 수 없다**.

### 11.2 Phase A 성공 판정 (확정)

Phase A 성공지표는 **정성 인터뷰 항목**으로 명시한다. 오픈 카운트를 완주율·CTA율 대리변수로 쓰지 않는다.

인터뷰 / 사용 관찰 체크리스트:

- [ ] 첫 화면을 “영업 사이트”가 아니라 “계산 도구”로 인식하는가  
- [ ] contract 결과(2×2)를 끝까지 이해하는가 (실수령 vs 통제가치 구분)  
- [ ] “검토받기”를 부담 없이 누르는가  

### 11.3 (선택) 최소 이벤트 계측 — 별도 동의 시에만

금액·입력값 없이 **이벤트명만** 저장. about/고지에 “이용 통계(페이지·버튼명)” 동의 문구 추가 후:

| event | 의미 |
|---|---|
| `contract_view` | 결과 영역 표시 |
| `contract_review_click` | 검토받기 |
| `contract_share_click` | 공유 |

미동의·미구축 시 Phase A는 §11.2만으로 충분. 퍼널 DB는 Phase C 이후.

---

## 12. Phase A 테스트 범위 (`test/contract-cases.js`)

공개 계산기 최소 **10~12건**. Quick 확장 테스트가 아니라 **contract-engine + gate** 기준.

| # | 케이스 | 기대 |
|---|---|---|
| T1 | 0원·음수·극단값 입력 | 클램프/거부, NaN·음수 실수령 없음 |
| T2 | 정산율 0% · 100% | 경계에서 전속 실수령 공식 일치 |
| T3 | 전속 우세 시드 | `verdict === lean_exclusive` |
| T4 | 독립 우세 시드 | `verdict === lean_solo` |
| T5 | \|r\| ≤ 5% 시드 | `verdict === similar` |
| T6 | 회사 부담 비용 반영 | 비용 변경 시 전속 결과에 **실제 차이** |
| T7 | 고액 소득 경비율 | 단순 29% 고정이 아닌 상세 경로(또는 상한) |
| T8 | 배당 없음 · 배당 2천만 경계 · 외부 금융소득 | 모드/증분세가 상세 엔진과 동일 원칙 |
| T9 | 실수령 ≠ 통제 경제가치 | 두 필드가 같고 섞인 단일 비교행이 없음 |
| T10 | 공유 URL | 쿼리에 금액·입력값 미포함 |
| T11 | `ref` 없음·만료·DB 오류 | `unavailable`/`expired`, UI 미노출 |
| T12 | (권장) 민감도 정렬 | `drivers[0]`가 최대 \|Δ\| 가정과 일치 |

기존 `node-run.js` 전체 회귀도 Phase A 완료 전 통과 유지.
