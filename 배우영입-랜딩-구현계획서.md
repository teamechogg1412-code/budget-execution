# 배우 영입용 랜딩 · Quick Simulator 구현계획서

- 문서 목적: 기존 「1인 기획사 예산 시뮬레이터」(`index.html` + `js/engine/*`)를 건드리지 않고, 그 앞뒤에 배우 영입용 공개 랜딩 퍼널을 붙이는 **웹 도구/링크 자체**의 구현 설계
- 범위 밖: 카피라이팅 톤앤매너, 회사소개 문구, 영업 전략 — 별도 피치덱에서 관리
- 작성일: 2026-08-23
- 상태: Phase 1 착수용 설계서 (개인별 링크 + 24시간 만료를 Phase 1로 승격)
- 배포: 정적 파일 + 최소 Supabase 테이블 1개 (링크 만료 판정용)

---

## 0. 기존 자산 재사용 원칙 (가장 중요)

이 저장소의 `index.html`은 이미 **한 명의 실제 배우용 비공개 도구**로 운영 중이다. 코드를 읽어 확인한 사실:

| 파일 | 현재 역할 | 공개용 랜딩에 그대로 가져가면 안 되는 이유 |
|---|---|---|
| `js/access.js` | 비밀번호(`1231`) 게이트 | 불특정 다수 배우에게 공유할 링크에 공용 비밀번호를 넣는 건 의미가 없음. **공개 경로에서는 완전히 제거.** 대신 §3의 개인별 링크(`ref`) + 24시간 만료가 접근 통제 역할을 대체 |
| `js/telegram.js` | 접속 시마다 기기·시간·URL을 조용히 텔레그램으로 전송하는 무음 빔 | 기기정보(User-Agent)·상세 URL까지 가져오는 건 "몰래 모니터링하지 않는다" 원칙과 충돌. **§0.1 `visit-ping.js`로 대체** |
| `js/data/sample-from-xlsx.js` | 기본 시드값 (초기현금 1.2억, 광고 회당 1.5~3억, 시딩·화보·앰버서더 단가 등) | 익명 이름("배우")이어도 특정 실제 딜의 요율 구조로 보임. 다른 배우에게 그대로 보여주면 안 됨 |
| `js/remote-store.js` + `js/supabase-config.js` | 로그인 유저의 예산 state를 통째로 원격 저장(현재 `enabled:false`) | 공개용에는 **입력 금액을 저장하는 용도로는 계속 비활성 유지**. §3의 만료 판정용으로만 별도 테이블 하나를 새로 씀 (아래 참고) |

**재사용 가능(계산 로직, UI 없이 순수 로직):**

```
js/money.js
js/month.js
js/calendar.js
js/holidays-kr.js
js/format.js
js/data/categories.js
js/data/personal-tax.js
js/data/corporate-tax.js
js/data/insurance-rules.js
js/engine/*.js  (revenue, expense, payroll, tax, cashflow, ledger, index, validation …)
```

이 파일들은 순수 계산 함수라 상태(state)만 새로 만들어 넣으면 그대로 돌아간다. **엔진 코드는 한 줄도 고치지 않는다.**

**절대 재사용 금지:** `access.js`, `telegram.js` 원본, `sample-from-xlsx.js`의 실제 값.

**구현 중 추가로 발견한 것:** `js/data/defaults.js`의 `ensureState`가 `revenueFees`가 비어 있으면 자동으로 `"써니스"(5%)`, `"메리디안"(15%)`이라는 이름의 기본 수수료 항목을 채워 넣는다(공유 엔진 파일에 하드코딩됨). 그대로 두면 reveal 페이지를 보기도 전에 상세 계산 화면에 "메리디안"이 비용 항목으로 노출되어 "처음부터 메리디안을 팔지 않는다" 원칙이 깨진다. → `js/data/public-seed.js`에 `revenueFees`를 명시적으로 채워(`"현재 소속사 수수료(예시)"`) 기본값 주입을 막았다. `defaults.js` 자체는 비공개 도구와 공유하므로 수정하지 않았다.

### 0.1 `visit-ping.js` (신규, `telegram.js`의 경량 대체)

```javascript
// landing/js/visit-ping.js
// 보내는 값: "방문이 있었습니다"만. User-Agent, 상세 URL, 기기정보 없음.
App.VisitPing.notify(pageName, ref);  // 예: notify("landing", "A8F2K")
```

- 전송 내용: 페이지 이름 + `ref` 코드 + 시각 정도. 브라우저 종류·화면 크기 등은 넣지 않는다.
- `about.html`에 "이 사이트의 방문 여부는 운영자에게 집계됩니다" 한 줄 고지.

---

## 1. 폴더 구조

```
/                          ← 기존 앱 그대로 (건드리지 않음)
  index.html
  js/, css/, ...

/landing/                  ← 신규 배우 영입 퍼널
  index.html               STEP 1 랜딩
  quick.html                STEP 2-3 Quick Simulation + Quick Result
  reveal.html               STEP 4-5 "실제 운영한다면?" 질문 + Meridian 소개 + CTA
  about.html                검증 페이지 ("이 사이트를 만든 사람들")
  contact.html              문의 폼. ref 없이도 항상 접근 가능 (만료 후 문의 경로)
  expired.html              링크 만료 화면 (아래 §3.3)
  css/landing.css           모바일 우선 별도 스타일
  js/quick-engine.js        Quick Simulation 전용 경량 계산 (§4)
  js/link-gate.js           ref 유효성 + 24시간 만료 판정 (§3)
  js/visit-ping.js          §0.1

/simulator/                 ← 공개용 "상세 계산" 진입점
  index.html                기존 index.html 복사본에서 아래만 제거/교체
```

`/simulator/index.html`은 루트 `index.html`을 복제한 뒤 다음만 바꾼다.

1. `access.js`, `telegram.js` 스크립트 태그 삭제
2. `sample-from-xlsx.js` 대신 `js/data/public-seed.js`(신규, 실제 클라이언트 값 없음)로 교체
3. `supabase-config.js`는 예산 state 저장 용도로는 `enabled: false` 유지 (만료 판정용 테이블 접근은 별도 최소 클라이언트로 처리)
4. `js/link-gate.js` 추가 — §3의 만료 판정을 이 페이지에서도 동일 적용
5. `js/export-excel.js`(기존 "엑셀 검산")는 그대로 재사용. 문구를 "엑셀로 저장"으로 강조
6. "최종 시드로 되돌리기" 문구는 "예시값으로 되돌리기"로 교체

---

## 2. 사용자 흐름

```
배우 A에게 개별 발송: https://.../landing/?ref=A8F2K
        │
        ▼
/landing/index.html?ref=A8F2K   ← link-gate.js가 먼저 24h 판정
        │ 유효                                    │ 만료
        ▼                                          ▼
   [직접 계산해보기]                        /landing/expired.html
        ▼
/landing/quick.html?ref=A8F2K  (Quick Simulation → Quick Result)
        │ [더 자세히 계산]         │ [상세 비교 보기]
        ▼                          ▼
/simulator/index.html?ref=A8F2K   /landing/reveal.html?ref=A8F2K
  (동일 link-gate 적용)                   │ (질문 → Meridian 소개 → CTA)
                                          ▼
                                 /landing/contact.html (ref 불필요, 항상 열림)
```

`about.html`, `contact.html`, `expired.html`은 `ref` 없이도 항상 접근 가능해야 한다 (문의 경로가 막히면 안 됨).

---

## 3. 개인별 링크(`ref`) + 24시간 만료

배우마다 링크를 따로 보내기 때문에, "처음 연 시점부터 24시간"이라는 기준 자체가 **그 배우의 ref 코드**로만 성립한다. 공용 링크에는 적용하지 않는다.

### 3.1 최소 테이블

`supabase-config.js`가 이미 있으므로 이를 재사용해 테이블 하나만 추가한다. 관리자 대시보드나 퍼널 단계 기록은 넣지 않는다 (그건 Phase 2).

```sql
campaign_links (
  ref            text primary key,   -- 예: 'A8F2K'
  first_opened_at timestamptz,       -- 최초 오픈 시각. null이면 아직 안 열림
  label          text                -- 내부 관리용 메모. 배우 본인에게는 노출 안 됨
)
```

- RLS: anon key로는 `ref`를 아는 사람만 자기 행을 upsert(최초 1회)·select 할 수 있도록 정책 설정. 목록 조회(전체 select)는 막는다.
- 이 테이블에는 **입력 금액이나 개인 식별정보를 넣지 않는다.** ref 코드와 시각뿐.

### 3.2 `link-gate.js` 동작

```javascript
// landing/js/link-gate.js
App.LinkGate.check(pageName).then(function (result) {
  // result.state: "no_ref" | "valid" | "expired"
});
```

1. URL에 `ref`가 없으면 `"no_ref"` → 게이트 없이 통과 (내부 데모/테스트용 접근)
2. `ref`가 있으면 `campaign_links`에서 해당 행 조회
   - 행이 없거나 `first_opened_at`이 null → 지금 시각으로 1회 기록 후 `"valid"`
   - `first_opened_at`으로부터 24시간 이내 → `"valid"`
   - 24시간 경과 → `"expired"`
3. `"expired"`면 도구 화면 대신 `expired.html`로 리다이렉트(또는 같은 페이지에서 만료 뷰 렌더링)
4. 페이지 내 모든 내부 링크(퀵 계산 → 결과 → 상세 계산 → reveal)는 `ref` 쿼리스트링을 그대로 이어붙여 전달한다

`/simulator/index.html`에도 동일하게 적용해, 상세 계산으로 새 탭을 열어도 만료 기준은 하나로 유지된다.

### 3.3 만료 화면 (`expired.html`)

```
이 링크는 유효 기간이 지났습니다.

다시 확인하고 싶으시다면
메리디안 컬렉티브로 문의해주세요.

[메리디안 컬렉티브에 문의하기]  → /landing/contact.html
```

개인 이름·전화번호를 화면이나 코드에 직접 박지 않는다 — **어차피 명함을 통해 개인 연락처는 이미 전달됐다고 보고**, 웹상에는 회사명(메리디안 컬렉티브) 단위로만 노출한다. 실제 담당자 연결은 `contact.html` 제출 후 내부적으로 처리.

### 3.4 자동저장에 대한 정확한 표현

"저장 기능이 없다"는 **서버(메리디안)에 값이 남지 않는다**는 뜻이지, 작업 중 화면이 날아간다는 뜻이 아니다. 두 가지를 구분한다.

| 구분 | 동작 | 문구 |
|---|---|---|
| 작업 중 임시 보존 | `localStorage`에 입력값을 켜 둠. 링크가 유효한 동안, 같은 브라우저에서 새로고침해도 입력 안 날아감 | 화면에 별도 언급 안 해도 됨 (당연한 UX) |
| 서버 영구 저장 | 하지 않음. 링크가 만료되면(§3.2) 어차피 도구 자체에 접근 불가 | "이 계산 결과는 메리디안 서버에 저장되지 않습니다. 세무사 등과 함께 확인하려면 엑셀로 저장해 전달하세요." |

즉 `/simulator/index.html`의 `localStorage` 자동저장은 **꺼도 되고 켜둬도 되지만, 켜두는 쪽을 권장** — 작업 편의성이 올라가고, 어차피 24시간 게이트가 실질적인 접근 제한 역할을 하므로 "영구 저장 안 함" 주장과 모순되지 않는다. 켜둘 경우 문구는 위 표의 "서버 영구 저장" 행 문구만 쓰면 정확하다.

---

## 4. Quick Simulation 계산 로직

기존 상세 엔진(`js/engine/*`)은 입력 항목이 수십 개라 Quick 단계의 7개 입력과 맞지 않는다. 세율·보험료율 데이터만 공유하는 별도의 경량 계산 함수를 만든다.

```javascript
// landing/js/quick-engine.js
App.QuickEngine.calculate({
  annualWorkIncome,        // 연간 작품 수익
  annualAdIncome,          // 연간 광고·기타 수익
  currentAgencyRate,       // 현재 소속사 정산 비율 (0~1)
  currentAgencyCoveredCost,// 현재 소속사가 부담하는 비용
  employeeCount,           // 예상 직원 수
  ownerSalary,             // 대표자 예상 급여
  otherOpCost              // 기타 예상 운영비
})
→ {
  currentContractNet,      // 현재 전속계약 예상 최종 수익
  soloAgencyNet,           // 1인 기획사 예상 개인 수령 + 법인 잔존가치
  difference,
  favorable: "current" | "solo"
}
```

- 세율은 `js/data/corporate-tax.js`, `js/data/personal-tax.js`의 브래킷을 그대로 import (숫자 이중관리 방지)
- 계산 전 과정은 브라우저 메모리 안에서만. **네트워크 요청 없음**
- `favorable` 판정은 실제 계산 결과를 따르며, 하드코딩으로 항상 `"solo"`가 나오게 만들지 않는다 (§5)

---

## 5. 중립성 검증 (구현 체크포인트)

1. 현재 소속사 정산 비율이 낮고 운영비가 큰 입력값 → "현재 전속계약 유지가 유리합니다" 문구가 실제로 나와야 함
2. 반대 조건 → "1인 법인 운영이 유리합니다" 문구가 나와야 함

`test/quick-cases.js`에 이 두 케이스를 회귀 테스트로 고정 (기존 `test/cases.js` 컨벤션 따름).

---

## 6. 개인정보·데이터 원칙 (확정 사항 총정리)

- 입력한 실제 금액은 **서버로 전송하지 않는다.** 계산은 클라이언트에서만.
- 작업 중 `localStorage` 임시 보존은 허용 (§3.4). 서버 영구 저장과는 구분되는 개념.
- 결과를 계속 보고 싶으면 **엑셀 내보내기**로 사용자가 직접 자기 컴퓨터에 저장 (기존 `export-excel.js` 재사용)
- 접속 알림(`visit-ping.js`)은 `ref` 코드 + 페이지 + 시각만. 기기정보·상세 URL 없음
- 링크 접근 통제는 비밀번호가 아니라 **개인별 ref + 24시간 만료**(§3)로 처리
- 만료 후 안내는 개인 연락처가 아니라 **회사명(메리디안 컬렉티브)** 단위로 노출, 실제 연결은 `contact.html` 제출을 거침
- `about.html`에 방문 집계·ref 만료 로직 존재를 한 줄 고지

---

## 7. 페이지별 최소 요구사항

### 7.1 `/landing/index.html`
- 첫 스크롤 없이 보이는 3요소: 제목, 한 줄 설명, CTA 버튼
- 하단 `Developed by Meridian Collective` (작게)

### 7.2 `/landing/quick.html`
- 입력 폼 7개 필드 (`inputmode="numeric"`)
- 제출 시 같은 페이지에서 결과 카드로 전환
- 결과: 3개의 큰 숫자 카드(현재 전속계약 / 1인 기획사 / 차이) + 중립 판정 문구
- CTA 2개: `[내 조건으로 더 자세히 계산하기]`(→ `/simulator/index.html?ref=...`, 새 탭), `[상세 비교 보기]`(→ `reveal.html?ref=...`)

### 7.3 `/landing/reveal.html`
- 질문 리스트 섹션 (작품 검토/계약 협상/언론 대응/스케줄/회계세무/장기 전략)
- Meridian 소개 섹션 (MANAGEMENT/PR/CAREER/BUSINESS/OPERATION 5블록)
- CTA: `[메리디안과 이야기하기]`, `[질문 남기기]` → `contact.html`

### 7.4 `/landing/about.html`
- 회사명, 대표자, 담당자, 사업자 정보, 연락처, 개인정보처리방침, 시뮬레이터 개발 주체 명시
- 방문 집계·ref 만료 로직 고지 문구

### 7.5 `/landing/contact.html`
- ref 없이 항상 접근 가능
- 이름/연락처/메모 입력 → `mailto:` 링크 또는 간단 폼
- 사용자가 자발적으로 제출한 정보이므로 §6 원칙과 충돌하지 않음

### 7.6 `/landing/expired.html`
- §3.3 문구 그대로. CTA `[메리디안 컬렉티브에 문의하기]` → `contact.html`

---

## 8. 모바일 우선 스타일

- `landing/css/landing.css`는 기존 `css/styles.css`를 import하지 않고 새로 작성
- 결과 카드 3개는 세로 스택 우선, 큰 숫자 중심
- 버튼은 44px 이상 탭 영역 확보

---

## 9. 성공 판단에 필요한 최소 계측 (확정)

**"링크를 열었는지"만 본다.** 퀵 계산 완료, reveal 열람 같은 단계별 기록은 넣지 않는다.

- 이건 이미 §3.1의 `first_opened_at`으로 충족된다 — 24시간 만료 판정을 위해 어차피 기록하는 값이라 **별도 컬럼 추가가 필요 없다.**
- 열었는지 여부 외에는 그 배우가 계산을 끝까지 했는지, 결과를 봤는지, Meridian 소개까지 갔는지는 **모른다.** 안 열었으면 안 본 것으로 간주하고, 열었으면 그 이후 행동은 추적하지 않는다.
- `about.html` 고지 문구도 이에 맞춰 단순하게: "이 링크가 열렸는지 여부만 운영자에게 기록됩니다."

---

## 10. Phase 1 구현 체크리스트

- [x] `landing/sql/campaign_links.sql` — 테이블 + RLS 정책 초안 (§3.1). **실제 Supabase 프로젝트에 아직 실행 안 함**
- [x] `landing/js/campaign-config.js` — ref 판정용 설정(현재 `enabled:false`). 값 채우고 켜야 실제 동작
- [x] `landing/js/link-gate.js` — ref 판정 + 24h 만료. **fail-closed로 동작**: Supabase 미설정 상태에서 `ref`가 붙은 링크는 `expiredUrl`로 넘어감. `ref` 없는 링크(로컬 테스트)는 통과
- [x] `landing/expired.html` — 만료 화면, 메리디안 컬렉티브 문의 CTA
- [x] `/landing/index.html` — 랜딩
- [x] `/landing/quick.html` + `js/quick-engine.js` — Quick Simulation/Result
- [x] `test/quick-cases.js` + `test/quick-node-run.js` — 중립성 회귀 테스트 3케이스, 전부 통과 확인
- [x] `/landing/reveal.html` — 질문 화면 + Meridian 소개 + CTA
- [x] `/landing/about.html` — 검증 페이지 + 고지 문구 (사업자등록번호·주소·이메일·대표자는 TODO 자리표시자, 직접 채워야 함)
- [x] `/landing/contact.html` — mailto 기반 문의, ref 없이 항상 접근 (수신 이메일 `CONTACT_EMAIL`은 TODO, 직접 교체해야 함)
- [x] `/simulator/index.html` — 기존 index.html 복제, access.js/telegram.js 제거(세션스토리지로 잠금화면 우회), link-gate 적용, `public-seed.js`로 시드 교체, 엑셀 내보내기 문구 강조
- [x] `js/data/public-seed.js` — 신규, 실제 클라이언트 값 없는 예시 시드. **defaults.js의 "메리디안/써니스" 기본 수수료 자동주입도 별도로 막음**
- [x] `test/public-seed-node-run.js` — 공개 시드가 상세 엔진에서 정상 동작하는지 + 실제 클라이언트 값 미포함 확인 (통과)
- [x] `landing/css/landing.css` — 모바일 우선 스타일
- [x] `landing/js/visit-ping.js` + `visit-ping-config.js` — 경량 방문 알림 (현재 `enabled:false`)

**아직 안 됨 / 실제 배포 전 채워야 하는 것:**
1. Supabase 프로젝트 생성 → `campaign_links.sql` 실행 → `campaign-config.js`에 url/anonKey 채우고 `enabled:true`
2. `about.html`의 대표자/사업자등록번호/주소/이메일 TODO 채우기
3. `contact.html`의 `CONTACT_EMAIL` 실제 수신 주소로 교체
4. 브라우저에서 `/landing/index.html`(ref 없이) 열어 전체 흐름 직접 확인 — 아직 로컬에서 눈으로 안 봄

## Phase 1에서 하지 않는 것 (Phase 2/3로 미룸)

- 관리자 대시보드(전체 배우 목록·퍼널 통계 화면)
- 공유하기 기능
- 결과 PDF 저장, 시나리오 저장
- 회원가입, 실명/연락처 강제 입력
