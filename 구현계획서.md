# 1인 기획사 법인 예산 시뮬레이터 구현계획서

- 문서 목적: 배우 1명이 1인 기획사를 설립·운영한다고 가정했을 때, 작품 계획과 회사 운영조건을 입력하면 **수익·비용·월별 현금흐름·필요 운전자금**을 즉시 계산하는 HTML 시뮬레이터의 구현 설계
- 원본: `예산_상세_update (1).xlsx` (시트 3장) — 업무 규칙·샘플 데이터·오류의 근거 자료
- 작성일: 2026-08-20
- 추가: 2026-08-21 §32 세금 귀속연도 분리
- 상태: 구현 착수용 설계서 (V1 범위 확정)
- 배포: 서버 없는 로컬 HTML. 설치 없음

---

## 0. 문서 읽는 법

이 문서는 두 층을 구분한다.

| 층 | 의미 | 구현에 쓰는 방식 |
|---|---|---|
| **원본 엑셀** | 현재 예산안의 스냅샷. 특정 작품명·특정 기간·특정 셀 공식 | 분석·샘플 시드·오류 목록. **제품 스키마가 아님** |
| **HTML 제품** | 1인 기획사 운영 시뮬레이터 | 조건 입력 → 엔진 계산 → 결과. **이것만 구현한다** |

원본의 `하렘의 남자들`, `언니내왕`, `광고`, `2026.11~2027.09`는 **샘플**이다.  
제품에 고정 행·고정 기간으로 넣지 않는다.

엑셀 수식을 JavaScript로 번역하지 않는다.  
업무 규칙을 다시 정의하고, 엑셀은 그 규칙의 참고 사례로만 쓴다.

---

## 1. 한 줄 요약

배우는 1명, 회사는 1개다.  
사용자는 작품·계약금액·입금시기·직원·급여·비용 조건만 넣는다.  
시스템이 월별 입금·비용·현금잔액·최저잔액·필요 운전자금·예상이익을 만든다.

엑셀형(월별 숫자를 칸에 채워 결과표를 만드는 것)이 아니다.  
시뮬레이터형(사업 조건 → 결과 자동 생성)이다.

이 프로그램은 여러 배우를 관리하는 매니지먼트 ERP가 아니다.

---

## 2. 왜 HTML로 바꾸나

엑셀 원본의 한계:

| 문제 | 실제 상태 |
|---|---|
| 입력과 공식이 한 시트에 섞임 | 월 손익 시트에 공식 397개, 숫자 입력은 73개 |
| 빈칸·오류가 결과 전체를 망가뜨림 | 9월 퇴직급여 `#REF!` → 월간 지출·통장잔액·법인세 열까지 전파 |
| 행을 추가하기 어려움 | 프로젝트·인건비 항을 넣으면 합계 범위를 수동으로 고쳐야 함 |
| 매출을 월 칸에 직접 씀 | 계약금·중도금·잔금 일정이 모델로 없음. 입금 시점을 바꾸려면 여러 칸을 고침 |
| 기간이 시트 열에 고정 | 2026.11~2027.09 11개월. 다른 연도를 보려면 열을 늘려야 함 |
| 시나리오 비교가 불편 | 전속 8:2 / 7:3 비교가 시트 우측에 고정 |
| 3번 시트는 데이터보다 메모 | 셀 3개 + 메모. 비교표 본문은 이미지로 보임 |

HTML로 바꿀 때 목표:

1. **사업 조건만 입력**하면 월별 결과표가 생긴다.
2. 빈칸·0·잘못된 문자여도 `#REF!` / `NaN` 없이 결과가 나온다.
3. 작품·직원·비용을 추가·삭제해도 합계가 배열 기준으로 따라간다.
4. 브라우저에서 파일만 열어도 동작한다. 설치·서버 불필요.
5. 현금유출과 손익비용을 구분한다. 보증금이 이익을 깎지 않는다.

---

## 3. 원본 시트 분석

이 장은 **원본 엑셀의 사실**이다. 제품 화면 설계가 아니다.

### 3.1 시트 구성

| 시트 | 역할 | 제품에서의 취급 |
|---|---|---|
| `1. 법인설립시 필요자금` | 일회성 설립비 + 보증금 + 에코 인큐베이팅 | **설립/자산/보증금 입력의 샘플 시드**. 화면을 이 시트 모양으로 복제하지 않음 |
| `2. 2026_예산` | 2026.11 ~ 2027.09 월별 손익·자금 | **운영 규칙·요율·샘플 계약의 근거**. 월 열을 제품 기간으로 고정하지 않음 |
| `3. 사무실 기준` | 공유오피스 vs 서초동 비교 메모 | **참고 메모만**. 계산 엔진에 넣지 않음 |

시트 간 연결은 한 곳뿐이다.

- 운영 예산 `초기셋팅 예산(11월)` = 설립 시트의 **보증금 합계(실제안)**
- 엑셀: `'1. 법인설립시 필요자금'!C5` → 실제로는 `F38` 합계의 부호 반전
- 현재 값: **-45,000,000원** (사무실 5백만 + 하이리무진 3천만 + 일반차량 1천만)

제품에서는 이 연결을 “11월 D18 셀 참조”가 아니라  
**보증금 항목의 지출월 = 시뮬레이션 시작월(또는 사용자가 지정한 지출월)** 로 다시 정의한다.

### 3.2 설립 자금 구조 (원본)

세 블록 + 맨 위 요약.

```
예상 필요 자금          (입력, 기본 6,000만)
- 법인 설립 필요 비용   (실제 예상 총액 합계)
- 각종 보증금           (환급성 자산, 실제안 합계)
= 예상 잔액
```

현재 계산값(엑셀 결과):

- 필요 자금 60,000,000
- 설립 비용(실제안) 1,861,900
- 보증금 45,000,000
- **예상 잔액 13,138,100**

항목 테이블 공통 컬럼:

| 컬럼 | 의미 | 입력/자동 |
|---|---|---|
| 구분 | 설립비용 / 시설장치 / 프로그램 등 | 고정 또는 선택 |
| 상세내역 | 등록면허세, 컴퓨터 … | 입력 |
| 금액 | 단가 | 입력 |
| 개수 | 없으면 1로 간주 | 입력 |
| 1안 제안 총액 | 시세·풀옵션 | 입력 (일부는 단가×개수) |
| 실제 예상 총액 | 이번에 집행할 금액 | 입력 또는 1안 복사 |
| 내역 | 메모 | 입력 |

블록:

1. **설립 필요 비용** (행 9–31) — 합계가 상단 “법인 설립 필요 비용”으로 올라감
2. **보증금** (사무실 / 하이리무진 / 일반차량)
3. **에코 인큐베이팅(워크스테이션 풀 패키지)** — 월 이용료 성격. 현재 실제안 합계 500,000원.  
   이 금액은 설립 잔액에는 안 들어가고, 운영 예산의 `물적인프라사용료`와 대응한다.

엑셀에서 이미 “에코에서 빌려 쓴다”고 0원 처리한 항목(컴퓨터, 책상, ERP, 그룹웨어 등)은  
제품에서 **포함/제외 토글**로 둔다. 예상가격을 지워서 0으로 만들지 않는다.

원본 설립 항목은 제품의 시드 목록일 뿐, 고정 마스터가 아니다. 사용자는 행을 추가·삭제한다.

### 3.3 2026 운영 예산 구조 (원본)

기간: **11월 ~ 다음해 9월 (11개월)**. 열 구성은 `TOTAL + 월`.  
이 기간은 원본 스냅샷이다. 제품의 기본 기간으로 강제하지 않는다.

세로 블록:

| 구분 | 항목 예 | 계산 성격 |
|---|---|---|
| 기초 자금 | 한화(기초잔액), 통장잔액 | 전월 기말잔액 이월 |
| 수입수수료 | 하렘의 남자들, 언니내왕, 광고 | 월별 입력 합계 = 매출 |
| 프로젝트 진행비 | 영화, 골드디거, 성사수수료, 트레이닝, AP, 영업비 | 월별 입력. 일부는 매출의 % |
| 인건비 | 대표, 영업, 로드매니저 | 월 급여. 이후 월은 첫 입력월 복사 |
| 4대보험 | 국민연금, 건강, 고용, 산재 | 인건비 합계 × 요율 |
| 고정성 비용 | 고문료, 재무/마케팅 아웃소싱, 임대, 인프라 | 월 입력 후 복사 |
| 차량 | 렌트 2대, 영업차량, 주유 | 렌트 1대는 VAT 포함가 환산 |
| 판관비 | 식대, 보험, 법인카드, 교통, 통신, 기장, 접대, 잡비, 퇴직 | 식대는 인원×단가×영업일 |
| 결과 | 월간 지출 합계 | 진행비 + 인건비 + 판관비(4대보험 포함) |
| 자금 | 통장 잔액 합계 | 기초 + 매출 + 지출 |

원본 매출 행은 **월 칸에 금액을 직접 기입**한다.

```
[원본 엑셀]
하렘의 남자들 / 언니내왕 / 광고
각 행에 월별 매출 직접 입력
```

샘플 입금(엑셀 계산값):

| 작품(샘플) | 월별 입력 | 연 합계 |
|---|---|---|
| 하렘의 남자들 | 12월 218,500,000 / 9월 11,500,000 | 230,000,000 |
| 언니내왕 | 12월 500,000,000 / 3월 400,000,000 / 8월 300,000,000 | 1,200,000,000 |
| 광고 | 2월 300,000,000 / 5월 300,000,000 / 8월 300,000,000 | 900,000,000 |

제품에서는 이 세 작품을 고정 행으로 두지 않는다.  
선택적 시드 JSON으로만 남기고, 화면은 **작품 등록 → 지급일정 → 월별 입금 자동 생성**이다.

우측 비교표 (전속계약 시나리오):

| 열 | 의미 | 현재 엑셀 결과 |
|---|---|---|
| P | 법인 자체 운영 (원안) | `#REF!` (9월 오류 전파) |
| Q | 전속 8:2 (수익 80%, 직접비 80%) | 실제 8.33억 / 마진 35.7% |
| R | 전속 7:3 + 비용 5:5 | 실제 7.83억 / 마진 33.6% |

세금 (원본):

- P열: **법인세** 누진세율 (2억 / 200억 / 3,000억 구간)
- Q·R열: **종합소득세** 누진세율 + 주민세 10%

전속 비교는 원본에 있으므로 분석은 남긴다.  
제품 V1의 핵심은 **1인 기획사 자체 운영**이다. 전속 8:2 / 7:3은 **V2**.

### 3.4 원본 수식 분석 (참고)

운영 시트에서 재사용할 **업무 아이디어**만 정리한다. 셀 주소를 제품에 심지 않는다.

| 원본 규칙 | 수식 예 | 제품에서의 재정의 |
|---|---|---|
| 전월 잔액 이월 | `E4=D48`, `F4=E48` … | `openingCash[m] = closingCash[m-1]` |
| 행 TOTAL | `C6=SUM(D6:N6)` | `sum(payments in range months)` |
| 매출 소계 | `C9=SUM(C6:C8)` | `sum(projects)` — 행 수가 변해도 동일 |
| 성사수수료 | `E13=-(E6*20%)-(E7*10%)` | 프로젝트별 `feeRate` + 기준(입금/계약총액) |
| 광고 AP | `G15=-G8*10%` (매출 있는 달만) | 프로젝트 `commissionRate`, 입금월에 발생 |
| 이후 월 복사 | `F19=E19` 다수 | 반복비용의 `startMonth~endMonth` + 월 override |
| 국민연금 | `(인건비합계*9%)/2` | `settings.insuranceRates.pension` |
| 건강보험 | `인건비*4.0674%` | `settings.insuranceRates.health` |
| 고용보험 | `인건비*1.15%` | `settings.insuranceRates.employment` |
| 산재보험 | `인건비*0.9%` | `settings.insuranceRates.industrialAccident` |
| 식대 | `-(12000*인원)*20` | Calendar Engine × 단가 × 인원. **20일 고정 폐기** |
| 차량 VAT | `-775390/1.1` | 항목의 `vatIncluded` 플래그. 확인 필요 |
| 법인세 | 2억/200억/3000억 구간 | `settings.tax.corporateBrackets` |
| 소득세 | 8구간 + 누진공제 | `settings.tax.incomeBrackets` — V2 전속 비교용 |
| 주민세 | 산출세액 × 10% | `settings.tax.localTaxRate` |

대조용 원본 계산값 (`#REF!` 없는 달):

| 월 | 매출 | 기말 통장잔액 |
|---|---|---|
| 11월 | 0 | 15,000,000 |
| 12월 | 718,500,000 | 569,315,446 |
| 1월 | 0 | 475,825,457 |
| 8월 | 600,000,000 | 1,354,395,534 |

이 숫자는 **엑셀 스냅샷 회귀**에만 쓴다.  
제품 정본 테스트는 아래 **25장의 합성 케이스**다. 엑셀 9월·P열은 깨져 있으므로 제품과 일치시킬 대상이 아니다.

### 3.5 엑셀에 이미 있는 오류 (제품에서 재현하지 말 것)

1. **N45 퇴직급여**  
   `=(N20+#REF!+#REF!+N21+#REF!)`  
   → 깨진 참조. 제품은 자동 예상 또는 월별 직접 입력. 미확정 시 전체 시뮬을 멈추지 않음.

2. **오류 전파**  
   N45 `#REF!` → N46 판관비 합계 → N47 월간 지출 → N48 통장잔액 → P50 이후 법인세·마진 전부 붕괴.  
   → 한 항목 오류가 엔진을 죽이지 않는다. 해당 항목만 0 + 경고.

3. **D17 vs E17 합계 범위 불일치**  
   11월 프로젝트 합계는 `D10:D12`, 12월부터는 `E10:E16`.  
   → 셀 범위 합계를 쓰지 않는다. `sum(array)`.

4. **설립 E17 개수 미적용**  
   법인인증서 단가 4,400 × 개수 2인데 1안 총액은 4,400.  
   → `예상금액 = 단가 × max(수량, 1)`. 실제 적용금액은 별도 필드.

5. **국민연금 `(인건비×9%)/2`**  
   상한·하한 없음. 원본과 같은 단순 요율은 V1 설정값으로 두되, UI에 **예산 추정용 단순 계산**을 표시. 정밀 상한은 V2.

6. **3번 시트**  
   계산 가능한 표가 아니다. 메모 텍스트만 참고. 이미지 복원 없음.

### 3.6 원본 → 제품 매핑 (고정 행을 만들지 않는 방법)

| 원본 | 제품 |
|---|---|
| 하렘의 남자들 / 언니내왕 / 광고 행 | `projects[]` 샘플 시드. UI 고정 행 아님 |
| 월 열 D~N (11월~9월) | `profile.startMonth` ~ `profile.endMonth`로 동적 생성 |
| 월 칸에 매출 기입 | `project.payments[]` → `calculateMonthlyRevenue()` |
| 식대 `인원×단가×20` | Calendar Engine 실적용일 |
| `=이전월` 복사 | 반복비용 기간 + override |
| 초기셋팅 = 보증금 합계 | 보증금 항목의 `payoutMonth` |
| 물적인프라 20만 | 월 고정 운영비 1건 (시드) |
| P/Q/R 전속 비교 | V2 시나리오 |
| 예상 필요 자금 6,000만 | `profile.initialCash` |
| 에코 0원 처리 | `include: false` + 사유 메모 |

---

## 4. 제품 정의

### 4.1 이 프로그램이 답하는 질문

> 이 배우가 1인 기획사를 차리고,  
> 드라마·영화·예능·광고 등을 몇 편/몇 건 진행하고,  
> 각각 얼마를 받고,  
> 계약금·중도금·잔금 등이 언제 들어오며,  
> 직원·사무실·차량·식대·프로그램·프로젝트 비용 등에 얼마를 쓰면,  
> 사업 시작에 얼마의 돈이 필요하고,  
> 월별 현금잔액은 어떻게 움직이며,  
> 돈이 가장 부족한 시점은 언제이고,  
> 최소 얼마의 운전자금이 필요하며,  
> 최종적으로 얼마의 이익이 남는가?

### 4.2 대상 사용자

- 배우 본인 또는 그 1인 기획사를 준비하는 실무자 1명
- 한 화면에서 **한 배우·한 법인·한 시뮬레이션**만 다룬다
- 배우 목록, 소속 아티스트 멀티 관리, 계약서 워크플로, 청구서 발행은 범위 밖

### 4.3 최상위 계산 흐름

```
배우 기본정보
    ↓
시뮬레이션 기간
    ↓
법인 설립 및 초기자금
    ↓
연간 활동계획 (목표 편수, 참고용)
    ↓
작품 / 광고 / 예능 / 행사 등 계약 등록
    ↓
계약금 / 중도금 / 잔금 등 지급일정
    ↓
월별 예상입금 자동 생성
    ↓
직원 및 인건비
    ↓
사무실 / 차량 / 식대 / 프로그램 등 운영비
    ↓
프로젝트 직접비
    ↓
월별 Cash Flow
    ↓
예상 세금
    ↓
월말 잔액
    ↓
최저 현금잔액
    ↓
필요 운전자금
    ↓
연간 예상 손익
```

### 4.4 절대 하지 말 것

1. 엑셀 셀 위치를 HTML 테이블로 복제하지 않는다.
2. `하렘의 남자들`, `언니내왕`, `광고`를 고정 행으로 만들지 않는다.
3. 사용자가 월별 매출을 일일이 직접 입력하게 만들지 않는다.
4. 2026년 11월~2027년 9월을 제품의 고정 기간으로 만들지 않는다.
5. 여러 배우를 관리하는 기능을 만들지 않는다.
6. 세율·보험료율·휴일을 여러 계산식에 하드코딩하지 않는다.
7. 원본의 잘못된 Excel 수식을 JavaScript로 번역하지 않는다.

---

## 5. 제품 원칙

### 5.1 입력과 결과의 역할

사용자는 결과표를 만들지 않는다.

입력: 작품, 계약금액, 입금시기, 직원, 급여, 비용 조건, 기간, 초기현금, 안전잔액.  
시스템: 월별 매출, 월별 비용, 현금흐름, 손익, 세금 추정, 최저잔액, 필요 운전자금.

자동 계산값은 읽기 전용이다. 사용자가 결과 칸을 고쳐서 공식을 덮어쓰지 않는다.  
예외가 필요하면 **입력 쪽**에서 한다 (월 override, 퇴직급여 직접 입력, 강제 근무일).

### 5.2 입력값 처리

| 입력 | 처리 |
|---|---|
| 빈칸 | 숫자 계산에서 0. 화면은 빈 칸 유지 가능 |
| 0 | 정상 값 |
| 음수 | 금액 입력폼은 양수만 받는다. 부호는 엔진이 붙인다 (입금+, 비용·보증금 현금−) |
| 문자/NaN | 해당 필드만 0 + 인라인 경고. 다른 계산은 계속 |
| 비율 | UI는 `%`, 내부는 `0~1` 소수. 저장도 소수 |

화면에 절대 내지 않는 값: `#REF!`, `NaN`, `undefined`, `Infinity`, 빈 합계 문자열.

표시 규칙: 계산 불가면 `0` 또는 `—` (분모 0인 마진율만 `—`). 절대 예외를 throw해서 화면을 하얗게 만들지 않는다.

### 5.3 합계

셀 범위(`SUM(C9:C31)`)를 쓰지 않는다.

```javascript
sumBy(items, fn)           // 배열 합
sumMonth(rows, month)      // 해당 월 발생액 합
```

행을 추가하면 다음 계산부터 자동 포함.

### 5.4 현금 vs 손익

`현금이 나갔다 = 비용`이 아니다.

| 구분 | Cash Out | P&L Expense |
|---|---|---|
| 급여, 보험, 식대, 임대, 수수료, 직접비 | 예 | 예 |
| 보증금 지출 | 예 | **아니오** |
| 자산구입(컴퓨터 등) | 예 | V1에서는 **현금유출만 반영, 감가상각 없음**. 손익에서는 `capex`로 분리 표시 |
| 설립 부대비용(등록면허세 등 소모성) | 예 | 예 |
| 추정 법인세 납부 (`taxCashOut`) | 예 (납부월을 지정한 경우만) | **아니오. 과세표준에 넣지 않음** |
| 보증금 반환 등 `otherInflow` | 현금 **입금** | **매출 아님** |

대시보드에 `월간 현금유출`과 `월간 비용`을 따로 보여 준다.

### 5.5 돈의 단위

- 내부: 항상 **정수 원**
- 입력: 숫자 (콤마 허용, 저장 시 제거)
- 표시: `600,000,000원` + 보조 `약 6억원`
- 반올림: 원 단위 `Math.round`. 요율 곱은 원 반올림 후 합산

### 5.6 배포·저장

- 서버·로그인·DB 없음
- `localStorage` 자동저장
- JSON import/export
- 민감 숫자(급여, 출연료)가 파일에 남는다. 앱 암호는 V1 없음. 사용 안내에 “로컬 파일·이 PC에만 저장”을 적는다

### 5.7 기술 형태

단일 진입점 `index.html`. 로직은 파일로 나눈다. 빌드 도구 없이 `<script>` 순서 로드.

전역 오염을 막는다. 각 파일은 IIFE로 감싸고, 공개 API는 `App` 아래로만 붙인다.

```javascript
window.App = window.App || {};
App.Month = App.Month || {};
App.Money = App.Money || {};
App.Calendar = App.Calendar || {};
App.Engine = App.Engine || {};
App.Store = App.Store || {};
App.Format = App.Format || {};
```

파일 최상단에 `var Foo = ...` / 맨 아래 맨살 함수를 두지 않는다.  
테스트 러너와 UI는 `App.Engine.runSimulation`만 호출한다.

React/Vite는 V1에서 쓰지 않는다. 로직이 커져도 먼저 엔진 파일을 쪼갠다.

---

## 6. 사용자 흐름

첫 실행:

1. 배우명·법인명·기간·최초현금·안전잔액
2. (선택) 원본 엑셀 샘플 시드 불러오기, 또는 빈 시뮬레이션
3. 설립비용·보증금·자산
4. 활동계획 편수 (참고)
5. 작품 추가 → 지급일정
6. 직원·운영비·식대
7. 대시보드에서 최저잔액·운전자금 확인
8. 조건 수정 → 즉시 재계산

이후 실행: localStorage 복원. 대시보드가 홈.

---

## 7. 화면 설계

회계 프로그램처럼 보이지 않는다.  
KPI 카드, 섹션 카드, 동적 행, 월 타임라인, 접기/펼치기, 경고.

추천 정보구조:

```
대시보드                         ← 홈, KPI + 현금 그래프 + 바로가기

사업 설정
 ├ 기본정보                      ← 배우/법인/기간/현금/안전잔액
 └ 활동계획                      ← 카테고리별 목표 편수 vs 등록 수

수익
 └ 작품 / 계약                   ← 동적 카드. 지급일정 포함

비용
 ├ 설립 비용
 ├ 보증금 / 자산
 ├ 인건비
 ├ 운영비                        ← 월 고정 + 일수 기반(식대)
 └ 프로젝트 직접비               ← 작품 카드 안에도 진입점

분석
 ├ 월별 Cash Flow                ← 동적 월 열. 월 클릭 시 입금/지출 명세
 └ 손익 / 세금                   ← 추정임을 명시

설정
 ├ 근무일 / 휴일
 ├ 보험요율
 └ 세율
```

공통 헤더: 배우명·기간, 마지막 저장 시각, `JSON 내보내기` / `가져오기` / `초기화`.

데스크톱 우선. 가로 스크롤은 Cash Flow 표만 허용.

---

## 8. 수익 / 작품 모델

### 8.1 배우는 1명

최상단 또는 사업 설정:

| 필드 | 키 | 기본 | 제약 |
|---|---|---|---|
| 배우명 | `profile.actorName` | `""` | 자유 입력. 필수 아님 |
| 법인명 | `profile.companyName` | `""` | 자유 입력 |
| 시작월 | `profile.startMonth` | 사용자 선택 | `YYYY-MM` |
| 종료월 | `profile.endMonth` | 시작월 + 11개월 | 시작월 이상. 권장 최대 36개월 |
| 최초 보유현금 | `profile.initialCash` | `0` | 정수 ≥ 0 |
| 최소 안전잔액 | `profile.safetyCash` | `0` | 정수 ≥ 0 |

기간이 바뀌면 Cash Flow 열이 다시 만들어진다.

지급건의 `expectedMonth`는 세 구간으로 나눈다. 월 Cash Flow 테이블에는 **기간 안**만 넣는다.

| 구간 | 조건 | 처리 |
|---|---|---|
| 기간 이전 | `expectedMonth < startMonth` | 월 테이블·기간 매출에 **넣지 않음**. KPI `기간 이전 입금`으로만 표시. **이미 `initialCash`에 들어 있다고 간주**. 자동으로 월초에 더하지 않음 |
| 기간 안 | start~end 포함 | 해당 월 `inflow` |
| 기간 이후 | `expectedMonth > endMonth` | 월 테이블·기간 매출에 넣지 않음. KPI `기간 이후 입금 예정액` |

기간 이전 지급이 있으면 경고 `payment_before_period`:  
`시작월 이전 입금 N원은 최초 보유현금에 이미 포함된 것으로 봅니다.`

### 8.2 활동계획 (매출이 아님)

카테고리 마스터 (`data/categories.js`):

`drama | ott | movie | variety | ad | pictorial | event | performance | other`

표시명: 드라마, OTT 시리즈, 영화, 예능, 광고, 화보, 행사, 공연, 기타.

```javascript
activityPlan: [
  { category: "drama", plannedCount: 2 },
  { category: "ad", plannedCount: 5 }
]
```

대시보드/수익 화면:

```
드라마   계획 2 / 등록 1
광고     계획 5 / 등록 3
```

등록 수 = `projects` 중 `status !== "cancelled"` 인 건수.  
이 숫자는 계산에 안 들어간다.

### 8.3 작품/계약 등록

`+ 작품/계약 추가` → `projects[]`에 객체 하나.

| 필드 | 설명 |
|---|---|
| `id` | 생성 시 UUID |
| `category` | 위 마스터 값 |
| `name` | 작품명/광고명. 미정이면 `2027 하반기 예상 드라마` 가능 |
| `status` | V1: `expected \| negotiating \| confirmed \| completed \| cancelled` |
| `contractAmount` | 총 계약금액 (원) |
| `shootStartMonth` | 선택. `YYYY-MM` |
| `shootEndMonth` | 선택 |
| `note` | 메모 |
| `payments[]` | 지급일정 |
| `directExpenses[]` | 프로젝트 직접비 |
| `fee` | 매출 연동 수수료. 없으면 null |

V1 상태 5개. V2에서 예상/제안/검토중/협의중/구두확정/계약예정/계약완료/촬영중/촬영완료/정산완료/무산으로 확장. 데이터는 문자열이라 호환 가능.

**취소** 작품은 입금·직접비·수수료 모두 0. 목록에는 남기고 취소 표시.

성사확률(`probability`)은 V2. V1 모델에 필드를 예약만 하고 UI에 안 낸다.

```javascript
probability: null  // V2. 0~1. null이면 미사용
```

V2 KPI 구분(지금은 설계만): 확정 매출 / 예상 포함 최대 / 확률가중.

### 8.4 월별 매출은 자동

사용자는 월 매출표를 채우지 않는다.

```
A 드라마
  계약금 1.2억 / 2027-01
  중도금 2.4억 / 2027-04
  잔금 2.4억 / 2027-09
광고 A
  계약금 1.5억 / 2027-03
        ↓
2027-01  120,000,000
2027-03  150,000,000
2027-04  240,000,000
2027-09  240,000,000
```

---

## 9. 지급일정 모델

총 계약금액과 현금 입금 시점은 반드시 분리한다.

### 9.1 지급 한 건

```javascript
{
  id: "",
  label: "계약금",          // 자유 입력. 프리셋 버튼으로 채울 수 있음
  inputMode: "amount",     // "amount" | "percent"
  amount: 0,               // 원. percent 모드면 계산 결과 캐시
  percentage: null,        // 0.2 = 20%. amount 모드면 null
  expectedMonth: "2027-01",
  actualDate: null,        // V2
  paymentStatus: "expected" // V1 고정. V2: expected|invoiced|partial|received|late
}
```

프리셋 라벨: 계약금, 중도금, 잔금.  
추가 예: 1차 중도금, 2차 중도금, 촬영 개시금, 촬영 완료금, 방영 후 잔금, 인센티브, 재방송료, 해외판매 정산, 기타.

`+ 지급 일정 추가`로 건수 제한 없음. 항목명 직접 입력.

### 9.2 금액 / 비율

- 금액 모드: `amount`가 입금액
- 비율 모드: `round(contractAmount * percentage)`
- `contractAmount`가 바뀌면 비율 모드 건만 재계산
- 금액 모드 건은 그대로

### 9.3 검증 (계산은 막지 않음)

```
지급합계 = Σ resolvedAmount(payments)
차이     = 지급합계 - contractAmount
```

- 차이 < 0: `지급계획이 계약총액보다 N원 부족합니다.`
- 차이 > 0: `지급계획이 계약총액보다 N원 초과합니다.`
- 차이 = 0: 표시 없음 또는 확인 표시

엔진은 `resolvedAmount`의 합으로 입금을 만든다. 부족해도 등록된 지급만 반영.

### 9.4 V1 / V2 입금

V1: `expectedMonth`만 사용. 그 월 1일에 전액 들어온 것으로 본다.  
모델에 `actualDate`, `paymentStatus`를 넣어 두고 UI는 숨긴다.

---

## 10. 비용 모델

엑셀 행 복제가 아니라 **계산 타입**으로 나눈다.

| type | 이름 | 공식 | 예 |
|---|---|---|---|
| `oneTime` | 일회성 | 해당 월에 amount | 설립, CI, 일회 출장 |
| `recurring` | 월 고정 | 기간 내 매월 amount, 월별 override 가능 | 임대, 렌터카, 기장, 통신 |
| `perHeadMonth` | 인원 × 월 단가 | headcount × monthlyRate | 월정액 복리후생 |
| `perDay` | 일 단가 × 일수 | dailyRate × workingDays(month) | 일 단위 차량 |
| `perHeadDay` | 인원 × 일 단가 × 일수 | 식대 | 식대, 현장식대 |
| `projectDirect` | 프로젝트 직접비 | 작품에 연결, 지정월 | 트레이닝, 스타일링 |
| `revenueLinked` | 매출 연동 % | 기준금액 × rate | 성사수수료, AP |
| `deposit` | 보증금 | 현금만, 손익 아님 | 사무실·차량 보증금 |
| `capex` | 자산구입 | V1 현금유출. 손익은 별도 표시 | 컴퓨터, 집기 |

공통 비용 추가 UI:

```
항목명
분류 (설립 / 인건비파생 / 판관비 / 프로젝트 / 보증금 / 자산)
계산 방식  ← type에 따라 필드 show/hide
시작월 / 종료월 또는 발생월
메모
[✓] 예산에 포함
```

`include: false`이면 예상금액은 남고 현금·손익에 안 넣는다. 사유 메모 권장.

### 10.1 반복비용과 월 override

```javascript
{
  id, name, category, type: "recurring",
  amount: 500000,
  startMonth: "2027-01",
  endMonth: "2027-12",
  include: true,
  overrides: { "2027-07": 2000000 }   // 7월만 200만
}
```

해당 월 금액 = `overrides[month] ?? amount`.  
기간 밖은 0.

### 10.2 프로젝트 직접비

작품 카드 안:

```
계약금액 / 직접비 합 / 기여이익(계약금액 - 직접비)
```

기여이익은 **입금 시점 무시 참고치**. 현금 기여는 Cash Flow에서 본다.  
V1은 작품 카드에 세 숫자. 상세 월별 프로젝트 P&L은 V2.

직접비 한 건: `name`, `amount`, `month`, `include`.

### 10.3 매출 연동 수수료

**V1은 `basis: "inflow"`만 구현한다.**  
계약총액에 한 번 곱하는 `basis: "contract"`는 발생월이 모호하므로 **V2**. V1 데이터에 `timingMonth`를 넣지 않는다.

```javascript
fee: {
  name: "성사수수료",
  rate: 0.10,
  amount: null,            // rate 대신 고정액이면 여기. rate와 동시에 있으면 amount 우선
  basis: "inflow"          // V1 고정. 그 달 예정입금 × 요율, 입금월에 비용 발생
}
```

산정:

```
fee[m] = round(그 달 해당 프로젝트 예정입금[m] * rate)
       또는 amount가 있으면, 입금이 있는 달에만 amount (고정액 분배는 V1 없음)
```

입금 0원인 달에는 수수료 0.

**확인 필요:** 원본은 하렘 20%+언니 10%를 **그 달 매출 칸**에 곱한다. 계약총액 일시 인식이 아니다. V1 `inflow`가 이 동작과 같다.

V2에서 `basis: "contract"`를 넣을 때 필수 필드: `timingMonth` (한 달). 없으면 구현하지 않는다. `fee.month`라는 키는 쓰지 않는다.

원본 광고 AP(매출 있는 달 10%)는 광고 프로젝트의 `fee`로 시드할 수 있다. 고정 행이 아니다.

---

## 11. 인건비

`employees[]` 동적 추가/삭제.

| 필드 | 설명 |
|---|---|
| `name` | 직책 또는 이름. 예: 대표이사, 매니저 |
| `role` | 자유 문자열 |
| `monthlySalary` | 양수 원 |
| `startMonth` / `endMonth` | 근무 기간. end 없으면 시뮬 종료월 |
| `insure` | 4대보험 적용 |
| `meal` | 식대 인원에 포함 |
| `severance` | 퇴직급여 대상 |
| `include` | 예산 포함 |

대표자도 같은 배열의 한 줄이다. 급여를 바꾸면 인건비·보험·퇴직·월지출·잔액·연간손익이 모두 다시 계산된다.

식대 적용 인원(월) = 그 월에 재직 중이고 `meal && include` 인 직원 수.  
별도 `mealExtraHeadcount`를 두면 직원 아닌 인원(현장 스태프 등)을 더한다.

### 11.1 4대보험

원본 분석은 3.4에 있다. 구현은 설정 객체만 읽는다.

```javascript
settings.insuranceRates = {
  pensionEmployer: 0.045,     // (9%/2). 원본과 동일 단순식
  health: 0.040674,
  employment: 0.0115,
  industrialAccident: 0.009,
  note: "예산 추정용 단순 요율. 상한·하한 없음"
}
```

```
보험대상급여[m] = Σ (insure && include && 재직) monthlySalary
국민연금[m] = round(보험대상급여[m] * pensionEmployer)
건강[m]     = round(보험대상급여[m] * health)
고용[m]     = round(보험대상급여[m] * employment)
산재[m]     = round(보험대상급여[m] * industrialAccident)
```

화면 설정에 **예산 추정용 단순 계산** 배지. 상한·하한·연령·특례는 V2.

**확인 필요:** 대표이사 보험 적용. V1 기본은 원본처럼 인건비 합계에 대표 급여를 넣고 `insure` 체크로 켠다.

### 11.2 퇴직급여

원본 `#REF!`를 가져오지 않는다.

```javascript
settings.severance = {
  mode: "manual",          // "auto" | "manual"
  autoMonths: 12           // auto일 때 월 적립 = 대상급여 / autoMonths
}
```

- `manual`: 월별 직접 입력 행 (`severanceManual[month]`). 빈칸 0
- `auto`: 그 월 퇴직대상 급여 합 / 12

미확정 기본: **`manual` + 전부 0**. 시뮬이 깨지지 않음.  
확인 후 `auto`로 바꿀 수 있다.

---

## 12. Calendar / 식대 계산

### 12.1 식대 공식

월 식대를 기본으로 직접 입력하지 않는다.

```
월 식대 = 1인 1일 식대 × 식대 적용 인원 × 실제 적용 근무일수
```

예: `15,000 × 3 × 21 = 945,000`

```javascript
mealSettings = {
  dailyRate: 15000,
  extraHeadcount: 0,
  calendarMode: "weekdaysExcludingHolidays"
  // weekdaysExcludingHolidays | weekdays | allDays | custom
}
```

적용 인원 = 직원 중 meal 인원 + extraHeadcount.

### 12.2 Calendar Engine

휴일 데이터를 식대 식에 박지 않는다.

```
js/calendar.js     근무일 계산
js/holidays-kr.js  연도별 공휴일 배열 (데이터만)
```

`calendarMode`:

| 모드 | 규칙 |
|---|---|
| `weekdaysExcludingHolidays` | 월~금, 토일 제외, KR 공휴일·대체공휴일 제외, 사용자 휴무 제외, 강제근무 포함 |
| `weekdays` | 월~금만. 공휴일이 평일이면 포함 |
| `allDays` | 그 월의 모든 날짜. 사용자 휴무 제외, 강제근무는 이미 포함 |
| `custom` | `settings.workWeek` 월~일 체크 + 공휴일 제외 여부 |

우선순위 (한 날짜):

1. `forcedWorkdays`에 있으면 **근무일**
2. 아니면 `customHolidays`에 있으면 **휴무**
3. 아니면 모드 규칙 + `holidays-kr`

### 12.3 대한민국 공휴일

오프라인 단일 HTML이 우선이므로 **지원 연도 JSON을 내장**한다.

- V1 내장 연도: **2026, 2027, 2028**
- 형식: `{ "2027-03-01": "삼일절", "2027-05-05": "어린이날", ... }` 대체공휴일 포함
- 그 외 연도: 공휴일 없이 요일만. 설정·경고 배너에 `holiday_year_missing`. **식대를 조용히 틀리게 두고 넘어가지 않는다**
- 이 경고는 V1 안전장치다. Case 15에서 반드시 검증한다

공공 API 실시간 호출은 V1에서 하지 않는다.

### 12.4 사용자 휴무 / 강제 근무

```javascript
customHolidays: [{ date: "2027-05-07", label: "회사 휴무" }]
forcedWorkdays: [{ date: "2027-05-05", label: "촬영" }]
```

### 12.5 계산 근거 UI

자동값 옆 `상세`.

```
2027년 3월 식대
평일                23일
공휴일 제외          1일
사용자 휴무          1일
추가 촬영근무        0일
실제 적용일          21일
15,000 × 3 × 21 = 945,000원
```

---

## 13. 설립자금

시트 1 구조를 **시드**로 살린다. 웹에서는 행 추가·삭제.

세 바구니:

1. `startupExpenses[]` — 소모성 설립비 (`oneTime`, P&L 비용)
2. `assets[]` — 컴퓨터·집기 (`capex`)
3. `deposits[]` — 보증금 (현금만)

각 항목:

| 필드 | 설명 |
|---|---|
| name, category | 항목명, 분류 |
| unitPrice, qty | 단가, 수량. qty 기본 1 |
| estimatedAmount | 기본 `unitPrice * max(qty,1)`. 덮어쓰기 가능 |
| actualAmount | 예산 반영액. 비면 estimated |
| include | 포함 토글 |
| month | 지출월. 기본 시뮬 시작월 |
| note | 예: 에코 제공 |
| excludeReason | include=false일 때 |

반영액 = `include ? (actualAmount ?? estimatedAmount) : 0`.

원본 “예상 필요 자금 6,000만 − 설립 − 보증금 = 잔액 1,313만”은  
**최초현금이 설립·보증금을 이미 뺀 금액인지, 빼기 전 금액인지**가 불명.

### 13.1 `initialCashTiming` — 구현자가 무시하면 안 되는 규칙

`profile.initialCash`는 **항상 첫 달 월초(`opening[0]`)에 그대로 넣는다.**  
타이밍 설정이 바꾸는 것은 월초 숫자가 아니라, **시작월의 설립/보증금/자산 지출을 cashOut에 넣을지**다.

시작월 일회성 항목 = `include`이고 `month === startMonth`인  
`startupExpenses` + `deposits` + `assets`.

| 값 | `initialCash`의 의미 | 시작월 설립·보증금·자산 |
|---|---|---|
| `beforeOutflows` (기본) | 그 지출을 **하기 전** 통장 | 시작월 `cashOut`에 **포함**. 월말에 차감됨 |
| `afterOutflows` | 그 지출을 **이미 한 뒤** 통장 | 시작월 `cashOut`에서 **제외**. 이미 월초에 반영된 것으로 본다 |

두 모드 모두:

- 시작월의 **운영비**(급여, 임대, 식대, 수수료 등)는 항상 cashOut에 넣는다. 설립성 일회 항목만 타이밍 대상이다.
- `month !== startMonth`인 설립/보증금/자산은 타이밍과 무관하게 해당 월 cashOut에 넣는다.
- `afterOutflows`인데 시작월 설립 항목이 있으면 경고 `startup_already_in_opening`:  
  `최초 보유현금이 설립·보증금·자산 지출 후 잔액입니다. 시작월 해당 지출은 현금흐름에서 빼지 않습니다.`

잘못된 구현 (금지):

1. `afterOutflows`인데 시작월 설립/보증금/자산을 또 빼서 **이중 차감**
2. `beforeOutflows`인데 시작월 설립/보증금/자산을 빼지 않아 **미차감**
3. `afterOutflows`를 “월초 = initialCash + 시작월 지출”로 재계산해 월초를 부풀림. 월초는 항상 `initialCash`

원본 6,000만 시드: `beforeOutflows` + 시작월에 설립/보증금 지출.

원본 인큐베이팅 월 50만은 `recurring` 운영비 시드 1건. 설립 잔액 공식에 넣지 않는다. 타이밍 대상도 아니다.

포함/제외 예:

```
컴퓨터  예상 3,000,000  예산반영 OFF  사유: 에코 제공
```

---

## 14. Cash Flow 계산

### 14.1 월 목록

```javascript
getSimulationMonths(startMonth, endMonth) → ["2027-01", "2027-02", ...]
```

문자열 비교로 달을 더하지 않는다. `js/month.js`:

- `addMonths(yyyyMm, n)`
- `diffMonths(a, b)`
- `isInRange(month, start, end)`
- `monthLabel(yyyyMm)` → `2027년 1월`

### 14.2 한 달

```
월초 현금
+ 해당 월 작품/광고 입금          ← inflow (매출)
+ 해당 월 기타 현금입금           ← otherInflow (매출 아님. 보증금 반환 등)
− 월 급여
− 4대보험
− 퇴직급여(설정된 경우)
− 식대
− 반복 운영비 (override 반영)
− 일수 기반 비용
− 프로젝트 직접비
− 매출 연동 수수료
− 해당 월 일회성 설립비           ← initialCashTiming 규칙 적용
− 해당 월 자산구입                ← 동일
− 해당 월 보증금                  ← 동일
− 해당 월 세금 납부               ← taxCashOut. pnlExpense에 넣지 않음
= 월말 현금
```

다음 달 월초 = 이번 달 월말.

첫 달 월초 = `profile.initialCash`. **예외 없음.** `afterOutflows`여도 월초를 재계산하지 않는다.

`otherInflow`는 매출이 아니다. 기간 매출·과세표준에 넣지 않는다.  
보증금 반환은 여기로만 넣는다. 작품 입금(`inflow`)으로 넣지 않는다.

### 14.3 월 결과 객체

```javascript
{
  month: "2027-04",
  opening: 0,
  inflow: 0,              // 작품 지급만. 매출
  inflowItems: [{ projectId, label, amount }],
  otherInflow: 0,         // 보증금 반환 등. 매출 아님
  otherInflowItems: [{ id, name, kind, amount }],
  payroll: 0,
  insurance: 0,
  severance: 0,
  meal: 0,
  mealBreakdown: { weekdays, holidaysExcluded, customOff, forcedOn, workingDays, headcount, dailyRate },
  recurring: 0,
  dayBased: 0,
  projectDirect: 0,
  fees: 0,
  oneTimeOpEx: 0,
  capex: 0,
  deposits: 0,
  taxCashOut: 0,          // 세금 현금 납부. 기본 0
  cashOut: 0,             // 운영지출 + deposits + capex + taxCashOut
  pnlExpense: 0,          // 세금·보증금·자산을 뺀 운영비용만
  closing: 0,
  belowZero: false,
  belowSafety: false
}
```

**`pnlExpense`에는 세금을 절대 넣지 않는다.**

```
pnlExpense = payroll + insurance + severance + meal + recurring
           + dayBased + projectDirect + fees + oneTimeOpEx
```

동치 (taxCashOut이 cashOut에 들어 있을 때):

```
pnlExpense = cashOut - deposits - capex - taxCashOut
```

`cashOut` 합산 전에 `pnlExpense`를 구하고, 그다음 `cashOut = pnlExpense + deposits + capex + taxCashOut` 순서가 안전하다.

### 14.4 세금 납부가 있을 때 계산 순서

순환 금지. 세금이 과세표준을 다시 낮추면 안 된다.

1. 월별 CF를 돌린다. 이 단계의 `taxCashOut`은 전부 0.
2. `기간 매출 = Σ inflow` (otherInflow 제외).  
   `기간 비용 = Σ pnlExpense`.  
   `과세표준 = max(0, 기간 매출 - 기간 비용)`.  
   세금 = `calculateEstimatedTax`.
3. `settings.tax.cashOutMonth`가 기간 안 월이면, **그 달에만** `taxCashOut`을 더하고 그 달부터 closing을 다시 이어서 계산한다.
4. 3번 이후 세금을 **다시 계산하지 않는다.** 현금만 움직인다.

`cashOutMonth === null`(V1 기본)이면 3번은 생략. 세금은 KPI에만 있다.

---

## 15. 손익 계산

기간 합:

```
기간 매출(입금 기준)     = Σ monthly.inflow          // 작품 지급만. otherInflow 제외
총 비용                  = Σ monthly.pnlExpense      // 세금 제외
예상 영업이익            = 기간 매출 - 총 비용
마진율                   = 매출===0 ? null : 영업이익/매출
예상 세금                = calculateEstimatedTax(영업이익 기반 과세표준)
세후이익                 = 영업이익 - 예상 세금
```

**확인 필요:** 원본은 발생주의(그 달 매출 칸)와 현금(입금)이 같다. 지급일정을 도입하면 **입금월 = 매출월**이 V1 정의다.  
미청구 잔금을 매출로 잡지 않는다. 발생주의는 V2.

KPI에 같이 보여 줄 것:

- 예상 계약총액 = Σ 취소 제외 `contractAmount`
- 해당 기간 예상 입금액 = 기간 내 `inflow` (작품)
- 기간 이전 입금 = 지급건 중 `expectedMonth < startMonth` (보유현금에 포함된 것으로 간주)
- 기간 이후 입금 예정액 = 지급건 중 `expectedMonth > endMonth`

계약총액 ≠ 기간 입금액일 수 있다. 둘 다 보여 준다.

---

## 16. 운전자금 계산

핵심 결과.

```
최저 예상잔액           = min(monthly.closing)
적자 보전               = max(0, -최저 예상잔액)
권장 추가 운전자금      = max(0, safetyCash - 최저 예상잔액)
월 평균 Burn Rate       = 기간 평균 (급여+보험+반복운영비+식대+일수비용)
                          프로젝트 직접비·수수료·일회성·보증금·자산 제외
```

예:

```
1월 60,000,000
2월 35,000,000
3월 12,000,000
4월 -18,000,000
5월 90,000,000

최저 예상잔액           -18,000,000
최소 추가 운전자금       18,000,000     (= 적자 보전)
안전잔액 5,000만이면
권장 추가 운전자금       68,000,000     (= 50,000,000 - (-18,000,000))
```

최저가 난 월 라벨도 KPI에 표시한다.

---

## 17. 세금

원본 법인세·소득세 식은 3.4에 보존한다.  
제품 세금은 **시뮬레이션 추정치**. 세무 신고용이 아님을 분석 화면에 고정 문구로 넣는다.

세율은 함수 본문에 숫자를 박지 않는다.

```javascript
settings.tax = {
  year: 2027,
  mode: "corporate",          // V1: 법인 법인세만
  cashOutMonth: null,         // null | "YYYY-MM". V1 기본 null
  localTaxRate: 0.10,
  corporateBrackets: [
    { upTo: 200000000, rate: 0.09, deduction: 0 },
    { upTo: 20000000000, rate: 0.19, deduction: 20000000 },
    { upTo: 300000000000, rate: 0.21, deduction: 420000000 },
    { upTo: Infinity, rate: 0.24, deduction: 9420000000 }
  ],
  incomeBrackets: [ /* 원본 8구간. V2 전속비교용으로 데이터만 적재 */ ]
}
```

V1:

```
과세표준 추정 = max(0, 기간 매출 - 기간 pnlExpense)
                // 기간 매출 = Σ inflow (otherInflow·세금 납부 제외)
법인세         = round(과세표준 * rate - deduction)   // 해당 구간
지방세(주민세) = round(법인세 * localTaxRate)
예상 세금      = 법인세 + 지방세
```

**금지:** `pnlExpense`나 과세표준 계산에 `taxCashOut` / 예상 세금을 넣지 않는다.  
세금이 세금을 깎는 자가공제가 된다.

세금은 **기간 말 한 번에 현금 차감하지 않는 것을 기본**으로 한다.  
(원본도 월 루프 밖 우측 표였다.)

`settings.tax.cashOutMonth = null | "YYYY-MM"`  
null이면 손익 KPI에만 반영, 월 현금은 안 깎음.  
지정 시 14.4 순서대로 그 달 `taxCashOut`에만 가산. 과세표준은 그대로.

전속 8:2 / 7:3 + 소득세는 **V2**. V1 메인에 표를 두지 않는다.

---

## 18. 데이터 모델

`state.version = 1`. import 시 version 없으면 1로 간주.

```javascript
state = {
  version: 1,

  profile: {
    actorName: "",
    companyName: "",
    startMonth: "2027-01",
    endMonth: "2027-12",
    initialCash: 0,
    safetyCash: 0
  },

  activityPlan: [
    { category: "drama", plannedCount: 0 }
  ],

  startupExpenses: [],
  deposits: [],
  assets: [],
  projects: [],
  employees: [],
  recurringExpenses: [],
  dayBasedExpenses: [],
  otherOneTimeExpenses: [],
  otherInflows: [
    // { id, name, amount, month, include, kind: "depositReturn" | "other" }
  ],
  severanceManual: {},
  mealExtraHeadcount: 0,

  customHolidays: [],
  forcedWorkdays: [],

  settings: {
    initialCashTiming: "beforeOutflows",
    meal: {
      dailyRate: 15000,
      calendarMode: "weekdaysExcludingHolidays",
      workWeek: [1, 2, 3, 4, 5]
    },
    insuranceRates: {
      pensionEmployer: 0.045,
      health: 0.040674,
      employment: 0.0115,
      industrialAccident: 0.009
    },
    severance: { mode: "manual", autoMonths: 12 },
    tax: { /* 17장. cashOutMonth 기본 null */ },
    currency: "KRW"
  }
}
```

프로젝트:

```javascript
{
  id: "",
  category: "drama",
  name: "",
  status: "expected",
  contractAmount: 0,
  shootStartMonth: null,
  shootEndMonth: null,
  note: "",
  probability: null,
  fee: null,  // V1: { name, rate, amount, basis: "inflow" } | null
  payments: [
    {
      id: "",
      label: "계약금",
      inputMode: "percent",
      amount: 0,
      percentage: 0.2,
      expectedMonth: "2027-01",
      actualDate: null,
      paymentStatus: "expected"
    }
  ],
  directExpenses: [
    { id: "", name: "트레이닝", amount: 0, month: "2027-02", include: true }
  ]
}
```

id는 `crypto.randomUUID()` 없으면 `timestamp-random` fallback.

---

## 19. 계산 함수

UI는 `runSimulation(state) → result` 한 번만 호출한다.  
내부에서 아래를 순서대로 부른다. 순수 함수. DOM 금지.

| 함수 | 입력 | 출력 |
|---|---|---|
| `getSimulationMonths(start, end)` | YYYY-MM | `string[]` |
| `resolvePaymentAmount(project, payment)` | 계약총액, 지급건 | 원 |
| `calculateProjectPayments(projects)` | projects | 지급건별 resolved + 총액차이 경고 |
| `calculateMonthlyRevenue(projects, months)` | | `{ [month]: { total, items[] } }` |
| `getHolidayMap(year)` | | `{ [date]: name }` |
| `calculateWorkingDays(month, settings, holidays, custom, forced)` | | `{ workingDays, breakdown }` |
| `calculateMealHeadcount(employees, extra, month)` | | number |
| `calculateDayBasedExpense(...)` | | 월별 금액 + breakdown |
| `calculatePayroll(employees, months)` | | 월별 급여 |
| `calculateInsurance(payrollByMonth, employees, rates)` | | 월별 4대보험 내역 |
| `calculateSeverance(...)` | mode, payroll, manual | 월별 금액 |
| `calculateRecurringExpenses(items, months)` | | 월별 합 + 항목 |
| `calculateProjectDirectExpenses(projects, months)` | | 월별 합 |
| `calculateRevenueLinkedFees(projects, inflowByMonth)` | | 월별 합. V1은 basis=inflow만 |
| `calculateOneTimeByMonth(list, months, startMonth, timing)` | 설립/자산/보증금 | 월별. `afterOutflows`면 시작월 건 제외 |
| `calculateOtherInflows(items, months)` | otherInflows | 월별 현금입금. 매출 아님 |
| `calculateMonthlyCashFlow(state, parts)` | taxCashOut=0인 1차 CF | `months[]` 14.3 객체 |
| `applyTaxCashOut(flows, tax, month)` | 1차 CF + 세금 | 납부월부터 closing 재계산. 세금 재계산 없음 |
| `calculateMinimumCashBalance(flows)` | | `{ min, month }` |
| `calculateRequiredWorkingCapital(minClosing, safety)` | | `{ deficitCover, recommended }` |
| `calculateBurnRate(flows)` | | 월 평균 고정 burn |
| `calculateProfit(flows, projects, range)` | | 매출=Σinflow, 비용=ΣpnlExpense, 기간전/후 입금 |
| `calculateEstimatedTax(profit, settings)` | | 과세표준, 법인세, 지방세. pnlExpense만 사용 |
| `runSimulation(state)` | 전체 state | `{ months, kpis, warnings, projectSummaries }` |
| `toSafeNumber(value)` | any | 유한 숫자, 아니면 0 |

`runSimulation` 최상단에서 state를 복사하지 않고 읽기만 한다.  
렌더러가 state를 바꾼 뒤 다시 호출한다.

모듈 파일:

```
js/money.js
js/month.js
js/calendar.js
js/holidays-kr.js
js/validate.js
js/engine/revenue.js
js/engine/expense.js
js/engine/payroll.js
js/engine/cashflow.js
js/engine/tax.js
js/engine/index.js          ← runSimulation
js/store.js                 ← localStorage, import/export
js/format.js
js/render/*.js              ← 화면
js/app.js                   ← 이벤트 연결
data/defaults.js            ← 빈 템플릿
data/sample-from-xlsx.js    ← 선택 시드 (원본 샘플)
```

---

## 20. Validation

엔진을 멈추지 않는 경고와, 필드 단위 차단을 나눈다.

### 20.1 필드 (저장은 하되 계산은 0)

- 금액/수량/인원에 숫자 아님 → 0 + “숫자를 입력해 주세요”
- 종료월 < 시작월 → 기간을 시작월 1개월로 보정 + 경고
- 비율 0~100% 밖 → clamp + 경고
- 지급월 형식 아님 → 그 건 입금 0 + 경고

### 20.2 경고 목록 (`result.warnings[]`)

| code | 조건 |
|---|---|
| `payment_short` / `payment_over` | 작품별 지급합 ≠ 계약총액 |
| `cancelled_with_payments` | 취소인데 지급이 있음 (0 처리 안내) |
| `payment_before_period` | 시작월 이전 입금. 월초 자동가산 없음 |
| `payment_after_period` | 종료월 이후 입금. KPI만 |
| `holiday_year_missing` | 공휴일 JSON 없는 연도. 요일만으로 식대 계산 |
| `cash_negative` | 어떤 월 closing < 0 |
| `cash_below_safety` | closing < safetyCash |
| `severance_manual_empty` | auto 아닌데 전 기간 0 (정보) |
| `fee_without_rate` | fee 객체는 있는데 rate/amount 없음 |
| `startup_already_in_opening` | `afterOutflows`이고 시작월 설립성 지출이 있음 |

경고는 배너 + 해당 카드. 계산은 계속.

### 20.3 화면 금지 값

`formatWon(n)` / `formatPct(n)`에서 비유한 숫자는 `0` 또는 `—`.  
`String(n)` 원본을 KPI에 그대로 넣지 않는다.

---

## 21. 저장 / Import / Export

### 21.1 localStorage

- 키: `solo-agency-budget:v1`
- 입력 디바운스 300ms 후 `JSON.stringify(state)`
- 로드 실패 시 빈 템플릿 + “저장 데이터를 읽지 못해 새로 시작합니다”

### 21.2 JSON

내보내기 파일명: `{법인명 또는 1인기획사}_예산안_{startMonth}.json`

가져오기: version 확인, 필수 키 없으면 거절 메시지.  
부분 JSON 병합 없음. 전체 교체.

### 21.3 초기화

`빈 시뮬레이션` / `원본 엑셀 샘플 불러오기` 두 가지.  
샘플은 `data/sample-from-xlsx.js`. 제품 스키마로 변환된 값만 넣는다 (셀 주소 없음).

### 21.4 Excel Export

V1 없음. V2 후보.  
V2를 할 때도 원본 레이아웃 재현 금지. 시트: 요약 / 월별 CF / 작품 / 직원 / 운영비 / 설립.

---

## 22. UI 설계

### 22.1 대시보드 KPI

**사업 규모**

- 예상 계약총액
- 해당 기간 예상 입금액
- 기간 이전 입금 (최초현금에 포함된 것으로 간주)
- 기간 이후 입금 예정액

**비용**

- 초기 설립비 (소모성)
- 인건비 (기간 합)
- 운영비
- 프로젝트 직접비
- 보증금
- 자산구입

**손익**

- 총 매출(입금 기준)
- 총 비용
- 예상 영업이익
- 예상 마진율
- 예상 세금
- 예상 세후이익

**현금**

- 최초 보유현금
- 최저 예상잔액 (월)
- 기간말 예상잔액
- 최소 필요 운전자금 (적자 보전)
- 권장 운전자금 (안전잔액 반영)
- 월 평균 Burn Rate

가능하면 최초 준비자금·월 고정비를 대시보드에서 바로 수정하거나, 클릭 시 설정 섹션으로 스크롤.

### 22.2 그래프

월말 현금 꺾은선/막대. SVG, 라이브러리 없음.

- 0 이하: 경고색
- 안전잔액 이하: 별도 표시 (0 이하와 구분)
- 안전잔액 수평 가이드 라인

### 22.3 월 타임라인

월 클릭:

```
2027년 3월
A 드라마 계약금     120,000,000
광고 A 잔금         150,000,000
총 예상입금         270,000,000
(지출 명세 접기)
```

### 22.4 입력 vs 계산

| | 스타일 |
|---|---|
| 입력 | 흰 필드, 테두리 |
| 자동 계산 | 읽기전용, 배경 구분, `자동` 라벨 |

자동칸을 contenteditable로 만들지 않는다.

### 22.5 톤

내부 의사결정용 시뮬레이터. 과한 장식 없음.  
용어: 매출·비용·현금잔액·운전자금. `차변/대변/선수금` 등은 쓰지 않는다.

---

## 23. V1 구현범위

실제로 쓸 수 있는 최소 완성제품.

포함:

- 배우명, 법인명, 시뮬 기간, 초기 현금, 최소 안전잔액
- 활동계획 편수 vs 등록 수 (참고)
- 설립비용 동적 행, 포함/제외, 단가×수량
- 보증금, 자산구입 분리
- 작품 동적 추가/삭제, 카테고리, 상태 5종, 계약총액
- 지급계획 (금액/비율, 프리셋+자유 라벨)
- 월별 예상입금 자동 생성
- 지급합계 vs 계약총액 경고
- 직원 추가/삭제, 월 급여, 근무기간, 보험/식대/퇴직 체크
- 월 고정 운영비, 기간, 월 override
- 식대 단가, 적용인원(직원 연동+추가인원)
- 주말/공휴일 근무일 엔진, 사용자 휴무, 강제 근무일
- 프로젝트 직접비
- 매출 연동 수수료 (입금 기준 %만. 계약총액 기준은 V2)
- 기타 현금입금 (`otherInflow`, 보증금 반환 등. 매출 아님)
- 월별 Cash Flow, 입금 명세
- 기간말 현금, 최저 현금, 필요/권장 운전자금, Burn Rate
- 입금 기준 손익, 법인세 추정 (현금 미차감 기본. 납부해도 pnlExpense 불변)
- `initialCashTiming` before/after 분기
- localStorage, JSON import/export
- 빈칸=0, NaN 차단, 동적 합계
- `test/runner.html` 자동 PASS/FAIL 요약 (아래 25장 케이스 전부)

빼는 것 (V1):

- 다중 배우, 로그인, 서버
- 성사확률, 실제 입금일, 청구 상태
- 매출 연동 수수료 `basis: "contract"` (발생월 필드 포함)
- 전속 8:2 / 7:3
- 시나리오 3안 비교
- 보험 상한, 세금 정밀, 감가상각, 부가세 신고
- Excel export, 엑셀 그리드 UI
- 3번 시트 이미지
- 모바일 전용 레이아웃

완료 기준:

1. `index.html`을 브라우저로 열면 빈 시뮬이 뜬다.
2. 작품 1개와 지급 3건을 넣으면 해당 월 입금이 생긴다.
3. 식대가 공휴일을 뺀 일수로 계산되고 근거가 보인다.
4. 잔액이 음수면 최저잔액·운전자금이 나온다.
5. 보증금은 현금을 줄이고 손익 비용에는 안 들어간다.
6. 모든 금액을 지워도 NaN이 없고 합계가 0이다.
7. 기간을 12→24개월로 바꾸면 열이 늘어난다.
8. 새로고침 후 입력이 남는다.
9. `test/runner.html`을 열면 25장 케이스가 자동 실행되고, 상단에 `PASS n / FAIL 0`이 나온다. FAIL이면 V1 미완료다.

---

## 24. V2 / V3 후보

### V2

- 성사확률, 확정/최대/가중 매출
- `actualDate`, 청구·일부입금·입금완료·지연
- 매출 연동 수수료 `basis: "contract"` + 필수 필드 `timingMonth`
- 사회보험 상한·하한, 대표 특례
- 세금 정밀화, 세금 납부월 현금 반영 UX
- 전속 8:2 / 7:3 비교 (원본 Q/R)
- 기준/보수/낙관 시나리오 저장·비교
- Excel 보고서 export
- 프로젝트별 월별 손익
- 공휴일 연도 확장
- 예산 vs 실제 집행

### V3 (별 시스템)

```
예산 → 계약 → 매출채권 → 실제 입금 → 비용 집행 → 손익 → 예산 대비 실제
```

이번 구현의 우선순위는 독립 HTML 시뮬레이터다.

---

## 25. 테스트 계획

엔진 테스트의 정본은 `test/runner.html`이다.  
페이지를 열면 아래 케이스를 **자동 실행**하고, 맨 위에 `PASS n / FAIL m`과 실패한 assert 메시지를 보여 준다.  
브라우저 콘솔에 수동으로 함수를 치는 것은 디버그용일 뿐, 완료 기준이 아니다.

러너 최소 구현:

```javascript
function assertEqual(name, actual, expected) { /* 불일치면 FAIL 목록에 추가 */ }
function assert(name, cond, detail) {}
function hasWarning(result, code) { return result.warnings.some(w => w.code === code); }
```

외부 프레임워크(jest 등)는 쓰지 않는다. 빌드도 없다.  
Phase 9 완료 조건: 이 페이지 FAIL = 0.

### Case 1 — 아무것도 입력하지 않음

빈 템플릿, 기간만 기본 12개월.  
모든 합계 0, 월말 = initialCash(0), 경고 없음(또는 정보 수준만), 예외 없음.

### Case 2 — 작품 1개

계약총액 600,000,000.  
1월 계약금 120,000,000 / 4월 중도금 240,000,000 / 9월 잔금 240,000,000.  
해당 월 inflow가 정확히 그 금액. 다른 월 입금 0. 기간 입금 합 6억.

### Case 3 — 지급계획 부족

총액 6억, 지급합 5.5억.  
경고 `50,000,000원 부족`. 입금은 5.5억으로 계산 계속.

### Case 4 — 식대

1인 15,000 × 3명.  
2027-03, 모드 `weekdaysExcludingHolidays`.  
결과 = 15,000 × 3 × 그 달 실적용일. breakdown의 workingDays와 일치.

### Case 5 — 휴일 촬영

Case 4에 공휴일 1일을 `forcedWorkdays`로 추가.  
식대 15,000 × 3 × 1일 증가.

### Case 6 — 현금 부족

초기현금 20,000,000. 월 고정비 15,000,000. 입금 없음 3개월.  
어느 월엔가 closing < 0. `min`과 `deficitCover`가 `-min`과 일치.

### Case 7 — 보증금

시작월에 보증금 50,000,000.  
그 달 cashOut에 포함, `pnlExpense`에는 미포함. 기간 영업이익이 보증금만큼 줄지 않음.

### Case 8 — 항목 추가

프로젝트·직원·운영비 각 1행 추가 후 합계 증가. 삭제 후 복귀.

### Case 9 — 빈칸

금액 클리어. NaN/`undefined` 문자열 없음. 0으로 계산.

### Case 10 — 기간 변경

12개월 → 24개월. `result.months.length === 24`.  
끝 12개월은 반복비용 종료월 이후 0 (종료월을 12월로 둔 경우).

### Case 11 — 포함 제외

설립 항목을 `include: true`, actualAmount 3,000,000으로 넣어 한 번 계산한다.  
그다음 **같은 항목만** `include: false`로 바꾼다.

기대:

- OFF 이후 현금유출·pnlExpense가 ON일 때보다 3,000,000 작다 (시작월 차감이 사라짐)
- `estimatedAmount` / `actualAmount` 필드 값은 그대로 3,000,000
- OFF 상태에서 estimatedAmount를 5,000,000으로 바꿔도 현금·비용은 **더 이상 변하지 않는다**

### Case 12 — 비율 지급

계약 6억, 계약금 20%. 계약총액을 3억으로 바꾸면 계약금 6,000만.

### Case 13 — JSON 왕복

state export → 초기화 → import. KPI 동일.

### Case 14 — 안전잔액

Case 6에 safetyCash 50,000,000.  
`recommended === max(0, 50000000 - minClosing)`.

### Case 15 — 공휴일 데이터 없는 연도

기간을 `2029-01` ~ `2029-12`로 둔다 (내장 공휴일 2026–2028만).  
식대 `weekdaysExcludingHolidays`.

기대:

- `holiday_year_missing` 경고가 있다
- 식대는 **요일만**으로 계산된다 (공휴일 제외 없이 월~금)
- 엔진 예외 없음. 식대가 NaN이 아님

### Case 16 — initialCashTiming 이중 차감 금지

시작월 2027-01. 보증금 50,000,000, month=2027-01. 운영비 없음. 입금 없음.  
initialCash 80,000,000.

`beforeOutflows`: opening 80,000,000, 1월 deposits 50,000,000, closing 30,000,000.  
`afterOutflows`: opening 80,000,000, 1월 deposits **0**, closing **80,000,000**.  
`startup_already_in_opening` 경고.

두 모드 모두 opening은 80,000,000이다. after에서 opening을 1.3억으로 만들지 않는다.

### Case 17 — 세금이 과세표준을 깎지 않음

기간 매출 100,000,000, pnlExpense 0, 세금 납부월을 기간 안 한 달로 지정.

기대:

- 납부 전후 `pnlExpense` 합이 같다
- 과세표준이 납부액만큼 줄어들지 않는다
- 납부월 `taxCashOut > 0`, 그 달 closing만 세금만큼 감소

### Case 18 — 기간 이전 입금

시작월 2027-01. 지급건 expectedMonth=2026-12, 금액 10,000,000.

기대:

- 1월 inflow에 포함되지 않음
- KPI 기간 이전 입금 = 10,000,000
- 월초가 10,000,000만큼 자동 증가하지 않음
- `payment_before_period` 경고

### Case 19 — 보증금 반환은 매출이 아님

`otherInflows`에 kind=`depositReturn`, 50,000,000, 어느 기간 안 월.

기대:

- 그 달 `otherInflow` 50,000,000, closing 증가
- 기간 매출(`Σ inflow`)은 불변
- 과세표준 불변

원본 엑셀 스냅샷 대조는 **선택**. 샘플 시드 로드 후 방향성만 확인. 9월·P열은 대조 대상 아님.

---

## 26. 구현 순서

엑셀 화면부터 그리지 않는다. 엔진 → 저장 → UI.

### Phase 1 — 기반

`money.js`, `month.js`, 빈 state, `toSafeNumber`, format.

### Phase 2 — 수익 엔진

작품, 지급 해석, 월별 입금, 지급 검증 경고.

### Phase 3 — 비용·직원

반복비, override, 일회성, 직접비, 급여, 보험, 수수료.

### Phase 4 — Calendar / 식대

`holidays-kr.js` 2026–2028, workingDays, meal breakdown.

### Phase 5 — Cash Flow

월 루프, opening/closing, cashOut vs pnlExpense.

### Phase 6 — 대시보드 / 운전자금

KPI, 최저잔액, 권장 운전자금, Burn Rate, 그래프.

### Phase 7 — 세금 추정

brackets 데이터 + 법인 추정. 신고용 문구.

### Phase 8 — 저장

localStorage, JSON, 빈 템플릿, 선택 샘플 시드.

### Phase 9 — Validation / 테스트

25장 케이스, `test/runner.html` 자동 PASS/FAIL. FAIL 0이 아니면 Phase 9 미완료.

### Phase 10 — UI 정리

7장 정보구조, 입력/계산 구분, 경고 배너, 사용 안내.

파일 산출:

| 파일 | 역할 |
|---|---|
| `index.html` | 셸 |
| `css/styles.css` | 레이아웃 |
| `js/*` | 19장 모듈 |
| `data/defaults.js` | 빈 state |
| `data/sample-from-xlsx.js` | 원본 샘플 시드 (선택) |
| `test/runner.html` | 엔진 케이스 자동 실행. PASS/FAIL 요약 |
| `구현계획서.md` | 이 문서 |

---

## 27. 구현 전 확인 필요사항

억지로 단정하지 않는다. V1은 fallback으로 구현하고, 아래가 정해지면 설정만 바꾼다.

| 항목 | 원본 상태 | V1 fallback |
|---|---|---|
| 퇴직급여 | N45 `#REF!` | `manual` + 0. 옵션으로 월급여/12 |
| 성사수수료 기준 | 그 달 매출 칸 × 작품별 % | 프로젝트 `fee.basis="inflow"`만. contract는 V2 |
| 4대보험 적용 범위 | 인건비 소계 전체 × 요율 | 직원별 `insure` 체크. 기본 ON |
| 대표이사 보험 | 인건비에 포함되어 같이 곱해짐 | 대표도 한 줄, `insure` 기본 ON |
| 세금 시뮬 기준 | 우측 표, 월 현금과 분리. P열은 `#REF!` | 기간 손익 추정, 현금 미차감. 납부해도 pnlExpense 불변 |
| 차량 VAT | `-775390/1.1` | 공급가(VAT별도) 입력. `vatIncluded` 토글 |
| 보증금 반환 | 모델 없음 | V1 자동 반환 없음. `otherInflows` (`kind: "depositReturn"`). 작품 `inflow`(매출)에 넣지 않음 |
| 최초 6,000만의 의미 | 필요자금 vs 통장잔액이 섞여 보임 | 13.1. 기본 `beforeOutflows`. 월초는 항상 `initialCash` |
| 식대 20일 고정 | `*20` | Calendar 실적용일. 20일 고정 폐기 |
| 전속 8:2 / 7:3 | 원본 핵심 비교 중 하나 | V2. V1 비표시 |
| 에코 인프라 20만 | 운영 행 | 월 고정비 시드 1건, 사용자가 수정 |
| 공휴일 없는 연도 | 해당 없음 | 요일만 계산 + 경고 |

이 목록 때문에 구현을 멈추지 않는다.

---

## 28. 샘플 시드 (선택)

`원본 엑셀 샘플 불러오기`를 눌렀을 때만 채운다. UI 기본 행이 아니다.

변환 원칙:

- 작품 3건 → `projects[]` + 월 입력을 `payments[]`로 재구성
- 직원 3건(대표/영업/로드매니저) → `employees[]`
- 설립 항목·보증금 → startup/deposits/assets, 에코 0원 항목은 `include:false`
- 기간 `2026-11`~`2027-09`, initialCash `60,000,000`
- 식대 12,000원, 인원은 직원 meal 체크로 재현 (원본 11월 5인·이후 6인은 extra/직원 입사월로 맞출 수 있으면 맞추고, 못 맞추면 extraHeadcount)
- 성사수수료·AP는 각 프로젝트 `fee`

시드 로드 결과가 엑셀 12월 잔액과 1원 단위로 같을 필요는 없다.  
지급일정 모델·캘린더 식대·퇴직급여 수정 때문에 **달라지는 것이 정상**이다.  
방향(12월 대규모 입금, 11월 보증금 유출)만 같으면 된다.

---

## 29. 다음 액션

이 설계서 기준으로 **Phase 1 (month/money/state)** 부터 구현한다.

구현 중 27장 미확정이 막으면 fallback을 코드에 넣고, 설정 화면에 노출한다.

---

## 30. V2 시나리오 비교 확장 구현계획

이 장은 V1의 1인 기획사 예산 시뮬레이터를 다음 목표로 확장하기 위한 계획이다.

목표:

> **동일한 배우 활동/매출 데이터**를 한 번만 입력하고,  
> 그 매출을 기준으로  
> A. 1인 기획사 법인 직접 운영  
> B. 기존 매니지먼트 회사 전속계약  
> 두 시나리오의 최종 배우 귀속 수익과 법인 잔여자금을 비교한다.

핵심 원칙:

1. 수익 데이터를 시나리오별로 복사하지 않는다.
2. 기존 V1 계산 결과를 `1인 기획사` 시나리오의 기본 결과로 사용한다.
3. 시나리오별로 달라지는 것은 **비용 부담, 수익배분, 세금, 개인 귀속 방식**이다.
4. `법인 잔여 현금`과 `배우 개인 실수령`은 절대 섞지 않는다.
5. 세금 정밀도가 부족한 단계에서는 사용자 입력 세율 또는 수동 세금 입력을 우선 지원한다.

구현 착수 전에 아래 6가지를 문서 SSOT로 고정한다. 코드가 이 규칙과 다르면 코드를 고친다.

| # | 규칙 | 이유 |
|---|---|---|
| 1 | 배분 공제 여부의 저장값은 `costBurdenRules` 하나다. `splitBasis`는 저장하지 않는다 | `총매출 기준`과 `배분 전 공제`가 동시에 있으면 구현자가 어느 쪽을 믿을지 모른다 |
| 2 | 공통 매출은 `soloResult.kpis.revenue`를 그대로 참조한다. 비교 엔진에서 다시 합산하지 않는다 | 매출연동수수료와 같은 이중 계산을 막는다 |
| 3 | `analysisMode`는 저장하지 않는다. `enabledScenarioIds`에서 렌더 시점에 파생한다 | 체크박스와 모드 문자열이 어긋날 수 있다 |
| 4 | `분석 > 시나리오 비교` 서브탭은 항상 있다. 0/1/2개 활성화에 따라 내용만 바꾼다 | 서브탭이 사라지면 사용자가 기능을 못 찾는다 |
| 5 | 대표이사 급여는 `soloResult.ledger.ceoSalary`를 재사용한다. 새 합계 로직을 만들지 않는다 | `js/engine/ledger.js`에 이미 `/대표/` 매칭 합계가 있다 |
| 6 | 전속 시나리오의 직원·판관비·초기비·직접비·수수료는 비용/수익 탭 기존 배열을 읽는다. 시나리오 전용 비용 배열을 만들지 않는다 | Layer 1 데이터를 복사하면 SSOT가 깨진다 |

### 30.1 정보구조

상위 탭은 유지한다.

```
대시보드
시뮬레이션 설정
수익
비용
분석
설정
```

확장 후 역할:

| 영역 | 역할 |
|---|---|
| `수익` | 공통 활동/매출 데이터. 작품·광고·행사·앰버서더·지급일정은 여기서 한 번만 입력 |
| `비용` | 1인 기획사 실제 운영비. 직원, 판관비, 초기비용, 보증금, 자산, 프로젝트 비용 |
| `시뮬레이션 설정` | 공통 조건 + 시나리오 선택 + 시나리오별 계산 규칙 |
| `분석` | 기존 월별 분석 + 신규 시나리오 비교 |

`분석` 안에는 서브탭을 둔다. 서브탭 자체는 항상 존재한다.

```
분석
 ├ 월별 분석
 └ 시나리오 비교
```

`시나리오 비교` 서브탭을 숨기거나 없애지 않는다. 활성화된 시나리오 수에 따라 내용만 바꾼다.

`시뮬레이션 설정` 안에는 아코디언 또는 서브탭을 둔다.

```
시뮬레이션 설정
 ├ 공통 조건
 ├ 1인 기획사
 └ 기존 회사 전속
```

처음부터 모든 입력란을 한 화면에 펼치지 않는다.

### 30.2 데이터 계층

세 계층을 분리한다.

| 계층 | 내용 | 예 |
|---|---|---|
| Layer 1 — 공통 활동/매출 | 시나리오에 무관한 배우 활동 데이터 | `projects[]`, `salesPlans[]`, `payments[]` |
| Layer 2 — 시나리오 규칙 | 같은 매출에 적용할 계산 규칙 | 1인 기획사 운영비, 전속 배분율, 비용 부담, 세금 입력 |
| Layer 3 — 결과 | 계산 산출물 | Cash Flow, 법인세, 개인세금, 배우 실수령, 비교 차액 |

기존 `projects[]`, `salesPlans[]`, `revenueFees[]`는 공통 데이터로 유지한다.  
전속 시나리오를 위해 별도의 `exclusiveProjects[]` 같은 중복 배열을 만들지 않는다.

### 30.3 상태 구조 초안

기존 JSON과 호환되도록 `settings.scenarios`를 선택적으로 추가한다.  
기존 JSON에 이 키가 없으면 V1과 같은 결과를 낸다.

```javascript
settings: {
  scenarioComparison: {
    enabledScenarioIds: ["soloAgency", "exclusiveContract"]
  },
  scenarios: {
    soloAgency: {
      label: "1인 기획사",
      usesCurrentOperatingModel: true,
      ownerPayout: {
        salaryEmployeeId: null, // null이면 ledger.ceoSalary 사용
        bonusAmount: 0,
        bonusMonth: null,
        dividendAmount: 0,
        dividendMonth: null
      },
      personalTax: {
        mode: "manual", // "manual" | "rate"
        manualTaxAmount: 0,
        effectiveRate: 0
      }
    },
    exclusiveContract: {
      label: "기존 회사 전속",
      companyShareRate: 0.30,
      actorShareRate: 0.70,
      costBurdenRules: {
        projectDirect: "deductBeforeSplit", // "company" | "actor" | "deductBeforeSplit" | "ignore"
        revenueLinkedFees: "deductBeforeSplit",
        payroll: "company",
        opex: "company",
        startup: "company",
        assetsAndDeposits: "company",
        actorPersonalCosts: "actor"
      },
      actorPersonalCosts: [], // 비용 탭에 없는 배우 개인 활동비만 여기 추가
      personalTax: {
        mode: "manual", // "manual" | "rate"
        manualTaxAmount: 0,
        effectiveRate: 0
      }
    }
  }
}
```

저장하지 않는 파생값:

| 파생값 | 계산 | 저장 금지 이유 |
|---|---|---|
| `analysisMode` | `enabledScenarioIds.length`가 0/1/2+이면 `none` / 단일 / `both` | 체크박스와 문자열이 어긋남 |
| `scenarios.*.enabled` | `enabledScenarioIds`에 해당 id가 있으면 true | 같은 켜짐 상태를 두 곳에 두면 SSOT가 깨짐 |
| `splitBasis` | `costBurdenRules`에 `deductBeforeSplit`이 하나라도 있으면 `netAfterDeductibleCosts`, 없으면 `grossRevenue` | 30.5 표의 “총매출 기준”과 “배분 전 공제”가 모순됨 |

`enabledScenarioIds`가 시나리오 활성 SSOT다.  
`costBurdenRules`가 배분 공제 SSOT다.

초기 구현에서는 `soloAgency`와 `exclusiveContract` 두 개만 둔다.  
향후에는 `scenarios[]` 배열로 확장할 수 있지만, V2 첫 단계에서는 현재 코드와 UI 복잡도를 줄이기 위해 고정 키를 사용한다.

구 JSON에 `analysisMode`, `splitBasis`, `scenarios.*.enabled`가 있으면 읽고 버린다.  
활성 여부는 `enabledScenarioIds`로만 복원하고, 배분 공제는 `costBurdenRules`로만 복원한다.

### 30.4 시나리오 A — 1인 기획사

현재 `runSimulation(state)` 결과가 1인 기획사 시나리오의 기본 결과다.

포함:

- 법인 상태: 신규 설립 / 기존 법인
- 최초 보유현금
- 초기비용
- 보증금 / 자산
- 직원 / 급여 / 식대 / 4대보험 / 퇴직급여
- 월 고정 운영비
- 프로젝트 직접비
- 매출 연동 수수료
- 법인세 추정
- 월별 Cash Flow

추가해야 할 구분:

| 항목 | 의미 |
|---|---|
| `법인 잔여 현금` | 현재 `kpis.endClosing` 중심. 법인 계좌에 남는 돈 |
| `배우 개인 실수령` | 대표자 급여, 상여, 기타 개인 귀속에서 개인세금 차감 후 |
| `배우 지배 경제가치` | 참고값. 개인 실수령 + 법인 잔여 현금. 개인 돈과 혼동하지 않게 표시 |

V2 초기 버전에서는 대표자 급여를 새로 합산하지 않는다.

우선순위:

1. 사용자가 `ownerPayout.salaryEmployeeId`로 지정한 직원의 기간 급여 합계. 기존 payroll/ledger 행 `emp-{id}`를 읽는다
2. 지정이 없으면 `soloResult.ledger.ceoSalary`를 그대로 쓴다. 이 값은 `js/engine/ledger.js`가 이름·역할에 `/대표/`가 있는 직원 급여를 이미 합산한 결과다
3. 둘 다 없으면 0

`ledger.ceoSalary` 계산식을 비교 엔진에 복제하지 않는다.

### 30.5 시나리오 B — 기존 회사 전속계약

기존 회사 전속 시나리오는 공통 매출을 가져와 배분한다.  
공통 총매출은 `soloResult.kpis.revenue`다. 전속 엔진에서 `projects[]`를 다시 순회해 합치지 않는다.

비용 숫자도 비용 탭 입력을 다시 받지 않는다.

| 전속 계산에 쓰는 값 | 출처 |
|---|---|
| 공통 총매출 | `soloResult.kpis.revenue` |
| 프로젝트 직접비 | `soloResult.kpis.projectDirect` |
| 매출연동 수수료 | `soloResult.kpis.revenueLinkedFeesTotal` |
| 급여·보험·퇴직 | `soloResult.kpis.payroll` |
| 판관비 | `soloResult.kpis.opex` |
| 초기 설립비 | `soloResult.kpis.startupCost` |
| 자산·보증금 | `soloResult.kpis.fundingOut` |
| 배우 개인 활동비 | `exclusiveContract.actorPersonalCosts`만. 비용 탭에 없는 항목 |

`costBurdenRules`는 위 금액을 **어디에 넣을지**만 정한다. 금액을 다시 계산하지 않는다.

기본 계산:

```
공통 총매출 = soloResult.kpis.revenue
배분 전 공제 = costBurdenRules가 deductBeforeSplit인 항목의 합
배분 기준금액 = 공통 총매출 - 배분 전 공제

배우 귀속 매출 = 배분 기준금액 × 배우 배분율
회사 배분 = 배분 기준금액 × 회사 배분율

배우 개인 실수령
= 배우 귀속 매출
- (costBurdenRules가 actor인 항목 합)
- 개인 세금
```

`company` 항목은 전속 시나리오에서 배우 실수령을 깎지 않는다.  
비교표의 판관비·급여 칸에는 “회사 부담”으로만 표시한다.

`splitBasis`와의 상호작용:

- 저장 필드 `splitBasis`는 없다.
- UI의 “배분 기준: 총매출 / 비용 차감 후”는 단축 토글이다. 누르면 `costBurdenRules`만 바꾼다.
- `비용 차감 후` → `projectDirect`, `revenueLinkedFees`를 `deductBeforeSplit`로 설정. 나머지 규칙은 유지.
- `총매출 기준` → 위 두 항목이 `deductBeforeSplit`이면 `company`로 되돌린다. 이미 `actor`인 항목은 건드리지 않는다.
- 토글 표시는 현재 `costBurdenRules`에서 파생한다. `deductBeforeSplit`이 하나라도 있으면 “비용 차감 후”, 없으면 “총매출 기준”.
- 개별 규칙을 수동으로 섞은 뒤에는 토글이 파생 라벨일 뿐이고, 계산은 항상 `costBurdenRules`를 따른다.

기본 설정:

| 필드 | 기본값 |
|---|---|
| 회사 배분율 | 30% |
| 배우 배분율 | 70% |
| 프로젝트 직접비 | 배분 전 공제 |
| 매출 연동 수수료 | 배분 전 공제 |
| 판관비 / 직원 / 사무실 / 차량 | 회사 부담 |
| 배우 개인 부담비용 | 0 |
| 개인세금 | 수동 0원 |

이 기본값의 파생 배분 기준은 “비용 차감 후”다.  
“총매출 기준”과 “직접비 배분 전 공제”를 동시에 기본값으로 두지 않는다.

배분율 검증:

- 회사 배분율 + 배우 배분율이 100%가 아니어도 저장은 허용한다.
- 계산 시 경고를 띄운다.
- UI에는 “합계 100% 맞추기” 버튼을 둔다.

### 30.6 비용 부담 규칙

전속계약에서는 같은 비용이라도 배우 개인에게 직접 차감하면 안 되는 항목이 있다.

초기 비용 카테고리:

| 카테고리 | 1인 기획사 | 기존 회사 전속 기본값 |
|---|---|---|
| 프로젝트 직접비 | 법인 부담 | 배분 전 공제 |
| 매출 연동 수수료 | 설정된 비용 분류에 반영 | 배분 전 공제 |
| 직원 급여 | 법인 부담 | 회사 부담 |
| 4대보험 | 법인 부담 | 회사 부담 |
| 식대 | 법인 부담 | 회사 부담 |
| 월 고정 운영비 | 법인 부담 | 회사 부담 |
| 초기 설립비 | 법인 부담 | 회사 부담 |
| 자산 / 보증금 | 법인 현금유출 | 회사 부담 |
| 배우 개인 활동비 | 별도 입력 없음 | 배우 부담 |

부담 방식 enum:

```javascript
"company"              // 기존 회사 부담. 배우 실수령 차감 없음
"actor"                // 배우 부담. 배우 몫에서 차감
"deductBeforeSplit"    // 배분 기준금액 계산 전에 공제
"ignore"               // 비교 계산에서 제외
```

초기 UI는 주요 카테고리 단위만 제공한다.  
항목별 세부 부담 설정은 V2.1로 미룬다.

### 30.7 개인세금

정밀 종합소득세 엔진을 V2 첫 단계에서 바로 만들지 않는다.

지원 방식:

| mode | 의미 |
|---|---|
| `manual` | 사용자가 예상 개인세금 금액을 직접 입력 |
| `rate` | 개인 귀속소득 × 사용자 입력 유효세율 |

초기 UI:

```
개인세금 방식
[ 수동 입력 ▼ ]

예상 개인세금        [        ] 원
```

또는:

```
개인세금 방식
[ 유효세율 ▼ ]

유효세율             [ 35 ] %
```

수동값이 있으면 수동값을 우선한다.  
세법 구간을 코드에 임의로 고정하지 않는다.

### 30.8 엔진 구조

기존 `App.Engine.runSimulation(state)`은 유지한다.

추가 엔진:

```javascript
App.Engine.runScenarioComparison(state)
```

내부 순서:

1. `var soloResult = App.Engine.runSimulation(state)`
2. 공통 매출은 다시 구하지 않는다. `soloResult.kpis.revenue`를 그대로 참조한다. 월별이 필요하면 `soloResult.months[].inflow`를 읽는다
3. `calculateSoloAgencyScenario(state, soloResult)`
4. `calculateExclusiveContractScenario(state, soloResult)`
5. `buildScenarioComparison(solo, exclusive)`

`runScenarioComparison`은 `runSimulation`을 감싸기만 한다.  
매출·급여·판관비·직접비 계산식을 비교 엔진에 복제하지 않는다.

함수 후보:

| 함수 | 역할 |
|---|---|
| `readCommonRevenue(soloResult)` | 매퍼. `soloResult.kpis.revenue` / `inflowInPeriod` / 월별 `inflow`를 읽기만 한다. 합산 금지 |
| `calculateSoloAgencyScenario(state, result)` | V1 KPI·ledger를 비교표용 항목으로 변환. `ledger.ceoSalary` 재사용 |
| `calculateExclusiveContractScenario(state, result)` | 전속계약 배분. 비용 숫자는 result KPI, 처리는 `costBurdenRules` |
| `calculateScenarioPersonalTax(income, settings)` | 수동/유효세율 기반 개인세금 |
| `buildScenarioComparison(a, b)` | 차액, 우위, 표시용 rows |

`calculateCommonRevenue`라는 이름으로 매출을 다시 더하는 함수는 만들지 않는다.

결과 구조:

```javascript
{
  scenarios: {
    soloAgency: {
      label: "1인 기획사",
      totalRevenue: 0,
      projectCosts: 0,
      opex: 0,
      corporateTax: 0,
      corporateEndingCash: 0,
      actorGrossIncome: 0,
      personalTax: 0,
      actorNetIncome: 0,
      controlledEconomicValue: 0
    },
    exclusiveContract: {
      label: "기존 회사 전속",
      totalRevenue: 0,
      deductibleBeforeSplit: 0,
      splitBase: 0,
      companyShare: 0,
      actorGrossIncome: 0,
      actorBorneCosts: 0,
      personalTax: 0,
      actorNetIncome: 0,
      controlledEconomicValue: 0
    }
  },
  deltas: {
    actorNetIncome: 0,
    controlledEconomicValue: 0
  },
  warnings: []
}
```

### 30.9 화면 설계

#### 30.9.1 시뮬레이션 설정

`시뮬레이션 설정`에 `비교 시나리오` 블록을 추가한다.

```
비교 시나리오
[x] 1인 기획사
[x] 기존 회사 전속
```

`분석 > 시나리오 비교` 서브탭은 항상 보여 준다.  
`enabledScenarioIds` 개수에 따라 내용만 바꾼다.

| 켜진 수 | 화면 |
|---|---|
| 0 | 빈 상태. “비교할 시나리오를 시뮬레이션 설정에서 켜세요” |
| 1 | 해당 시나리오 단일 요약. 비교표·차액 없음 |
| 2 | 좌우 비교표와 차액 |

서브탭을 비활성·숨김 처리하지 않는다.

`1인 기획사` 설정:

- 대표자 개인 귀속 기준
- 대표자 급여 직원 선택
- 상여 / 기타 개인 귀속
- 개인세금 방식

`기존 회사 전속` 설정:

- 회사 배분율
- 배우 배분율
- 배분 기준 토글(저장값 아님. `costBurdenRules`만 변경)
- 비용 부담 규칙
- 배우 개인 부담비용
- 개인세금 방식

#### 30.9.2 분석

기존 분석 화면은 `월별 분석`으로 유지한다.

새 `시나리오 비교` 화면:

| 항목 | 1인 기획사 | 기존 회사 전속 | 차이 |
|---|---:|---:|---:|
| 총 매출 | 0 | 0 | 0 |
| 프로젝트 비용 | 0 | 0 | 0 |
| 판관비 | 0 | 배우부담 0 | 0 |
| 회사 수익배분 | - | 0 | - |
| 배우 귀속 매출 | 0 | 0 | 0 |
| 법인 세전이익 | 0 | - | - |
| 법인세 | 0 | - | - |
| 개인 귀속소득 | 0 | 0 | 0 |
| 개인세금 | 0 | 0 | 0 |
| 배우 개인 실수령 | 0 | 0 | 0 |
| 법인 잔여 현금 | 0 | - | - |
| 실질 경제가치 | 0 | 0 | 0 |

표 아래에는 핵심 차액을 강조한다.

```
개인 실수령 차이
1인 기획사 - 기존 회사 전속 = +XXX원

실질 경제가치 차이
1인 기획사 - 기존 회사 전속 = +XXX원
```

### 30.10 구현 순서

한 번에 세금 엔진까지 만들지 않는다.

#### Phase V2-1 — 설정 스키마 추가

- `settings.scenarioComparison`
- `settings.scenarios.soloAgency`
- `settings.scenarios.exclusiveContract`
- 기존 JSON migration fallback
- 저장/불러오기 왕복 테스트

완료 기준:

- 기존 JSON을 불러도 V1 KPI가 변하지 않는다.
- 새 JSON에는 시나리오 설정이 저장된다.
- 저장 JSON에 `analysisMode`, `splitBasis`, `scenarios.*.enabled`가 없다.

#### Phase V2-2 — 시뮬레이션 설정 UI

- 비교 시나리오 체크박스 → `enabledScenarioIds`만 갱신
- 기존 회사 전속 배분율 입력
- 배분 기준 토글 → `costBurdenRules`만 갱신. `splitBasis` 저장 없음
- 개인세금 방식 입력

완료 기준:

- 입력값이 저장되고 새로고침 후 유지된다.
- 아직 비교 계산이 없어도 V1 화면은 깨지지 않는다.

#### Phase V2-3 — 비교 엔진 1차

- `runScenarioComparison(state)` 추가
- 공통 총매출은 `soloResult.kpis.revenue`를 참조한다. 비교 엔진에서 재합산하지 않는다
- 1인 기획사 시나리오는 기존 KPI·`ledger.ceoSalary`를 매핑한다
- 기존 회사 전속 1차는 총매출 × 배우 배분율. 비용 부담은 V2-4
- 개인세금은 수동 / 유효세율만 지원한다

완료 기준:

- 같은 매출을 두 번 입력하지 않는다.
- 배우 70%, 회사 30% 설정 시 배우 귀속 매출이 `soloResult.kpis.revenue`의 70%다. V2-3은 아직 공제 없음.

#### Phase V2-4 — 비용 부담 규칙

- 프로젝트 직접비 / 매출연동 수수료 / 판관비 / 배우 개인 부담비용 반영
- `deductBeforeSplit`, `actor`, `company` 분기

완료 기준:

- 같은 비용 데이터라도 기존 회사 전속에서는 회사 부담 비용이 배우 실수령을 깎지 않는다.
- `deductBeforeSplit` 비용은 배분 기준금액을 줄인다.

#### Phase V2-5 — 분석 서브탭

- `분석 > 월별 분석`
- `분석 > 시나리오 비교`
- 좌우 비교표
- 개인 실수령 / 법인 잔여 현금 분리 표시

완료 기준:

- 기존 월별 분석은 그대로 볼 수 있다.
- `시나리오 비교` 서브탭은 시나리오 0/1/2개여도 남아 있다.
- 시나리오 비교표에서 두 시나리오의 총매출은 동일하고, 둘 다 `soloResult.kpis.revenue`와 같다.

#### Phase V2-6 — 테스트

추가 테스트 케이스:

| 케이스 | 기대 |
|---|---|
| 기존 JSON 로드 | V1 KPI 불변 |
| 구 JSON `analysisMode`/`splitBasis` | 읽고 버림. 활성은 `enabledScenarioIds`, 공제는 `costBurdenRules` |
| 공통 매출 10억, 배우 70%, 공제 없음 | 기존 회사 배우 귀속 매출 7억. 양쪽 총매출 = `kpis.revenue` |
| 회사 부담 판관비 | 배우 실수령 차감 없음. 판관비 숫자는 `kpis.opex` |
| 배분 전 공제 1억 | 배분 기준금액 9억. `splitBasis` 필드 없이 `costBurdenRules`만으로 계산 |
| 대표이사 급여 | `ledger.ceoSalary`와 동일. 비교 엔진 자체 합산 없음 |
| 개인세금 수동 2억 | 배우 개인 실수령에서 2억 차감 |
| 개인세금 유효세율 30% | 개인 귀속소득 × 30% |
| 1인 기획사 | 법인 잔여 현금과 배우 개인 실수령이 별도 표시 |
| 시나리오 0개/1개 | 비교 서브탭은 남고 내용만 빈 상태/단일 요약 |
| 두 시나리오 비교 | 차액이 `A - B`로 정확히 계산 |

### 30.11 하지 말 것

- 전속 시나리오용 작품/매출 입력 화면을 따로 만들지 않는다.
- 기존 `projects[]`를 복사해서 시나리오별로 저장하지 않는다.
- 전속 시나리오용 직원/판관비/초기비 배열을 만들지 않는다.
- `splitBasis`, `analysisMode`, `scenarios.*.enabled`를 저장하지 않는다.
- 비교 엔진에서 공통 매출·대표이사 급여를 다시 합산하지 않는다.
- `분석 > 시나리오 비교` 서브탭을 숨기지 않는다.
- 종합소득세 누진세율을 임의 하드코딩하지 않는다.
- 법인 잔여 현금과 배우 개인 실수령을 하나의 값으로만 표시하지 않는다.
- 기존 비용/수익/Cash Flow/JSON 구조를 깨지 않는다.

### 30.12 V2 완료 기준

1. 공통 매출을 한 번 입력하면 두 시나리오가 동시에 계산된다.
2. `1인 기획사` 시나리오는 기존 V1 결과와 일관된다.
3. `기존 회사 전속` 시나리오는 배분율과 비용 부담 규칙을 반영한다.
4. 비교표에서 총매출은 양쪽이 동일하다.
5. 배우 개인 실수령, 법인 잔여 현금, 실질 경제가치를 분리해서 보여준다.
6. 기존 JSON import/export가 깨지지 않는다.
7. `test/runner.html` 또는 `node test/node-run.js`에서 신규 케이스 포함 FAIL 0이다.

---

## 31. 배우별 저장 및 웹 DB 전환 계획

현재 제품은 한 화면에서 한 배우의 예산 시뮬레이션을 다룬다.  
하지만 웹에 올려 여러 배우의 데이터를 다시 불러오려면 저장 단위를 명확히 분리해야 한다.

핵심 원칙:

1. 지금의 계산 state를 쪼개지 않는다.
2. **배우 1명 = 예산 시뮬레이션 state JSON 1개**로 저장한다.
3. 수익/비용/설정/Cash Flow 입력값은 배우별 state 안에 들어간다.
4. 배우 목록과 선택만 state 바깥의 인덱스로 둔다.
5. 웹 DB를 붙이더라도 기존 JSON 내보내기/가져오기는 유지한다.

### 31.1 왜 배우별 저장이 필요한가

현재 `localStorage`는 마지막 작업본 1개를 저장한다.  
브라우저를 열면 그 작업본을 바로 복원하는 구조다.

웹 서비스가 되면 다음이 필요하다.

| 필요 기능 | 설명 |
|---|---|
| 배우 목록 | 사용자가 관리하는 배우 또는 예산안 목록 |
| 배우별 불러오기 | 특정 배우를 선택하면 해당 state를 로드 |
| 배우별 자동저장 | 수정하면 해당 배우 state만 업데이트 |
| 권한 | 다른 사용자가 해당 배우 데이터를 볼 수 없게 제한 |
| 백업 | JSON export/import 유지 |

지금은 배우가 1명이므로 UI는 복잡하게 만들 필요가 없다.  
다만 저장 구조는 배우 단위로 확장 가능하게 열어 둔다.

### 31.2 저장 단위

현재 전체 state를 그대로 저장 단위로 사용한다.

```javascript
{
  id: "budget_state_...",
  actorId: "actor_...",
  ownerUserId: "user_...",
  title: "이종원 1인 기획사 예산안",
  state: {
    version: 1,
    profile: {},
    projects: [],
    revenueFees: [],
    startupExpenses: [],
    deposits: [],
    assets: [],
    employees: [],
    recurringExpenses: [],
    settings: {}
  },
  createdAt: "2026-08-20T00:00:00.000Z",
  updatedAt: "2026-08-20T00:00:00.000Z"
}
```

초기 웹 DB 버전에서는 내부 state를 정규화하지 않는다.  
예를 들어 `projects`, `employees`, `expenses`를 별도 테이블로 나누지 않는다.

이유:

- 현재 엔진은 state 전체를 한 번에 받아 계산한다.
- 부분 테이블로 나누면 저장/불러오기/마이그레이션 복잡도가 커진다.
- 아직 한 배우·한 예산안 단위가 제품의 자연스러운 경계다.

### 31.3 state 내부 meta

서버가 붙기 전이라도 state에 선택적 meta를 둘 수 있다.

```javascript
meta: {
  actorId: null,
  budgetId: null,
  ownerUserId: null,
  title: "",
  createdAt: null,
  updatedAt: null,
  storageMode: "local" // "local" | "remote"
}
```

주의:

- `meta`는 계산에 쓰지 않는다.
- `runSimulation`은 `meta`를 읽지 않는다.
- 기존 JSON에 `meta`가 없어도 정상 동작한다.

### 31.4 로컬 단계 구현

웹 DB를 붙이기 전 단계에서 먼저 할 일:

1. `state.meta` 기본값 추가
2. localStorage 키 구조를 현재 1개 저장에서 “현재 선택 예산안” 구조로 확장 가능하게 준비
3. JSON export에 meta 포함
4. JSON import 시 meta가 없어도 자동 보정

초기 localStorage 구조:

```javascript
solo-agency-budget:v2:currentId = "local-default"
solo-agency-budget:v2:items = {
  "local-default": {
    id: "local-default",
    title: "이종원 예산안",
    state: {}
  }
}
```

단, UI는 당장 배우 목록을 만들지 않아도 된다.  
현재는 `local-default` 하나만 사용한다.

### 31.5 웹 DB 후보

| 방식 | 장점 | 단점 | 판단 |
|---|---|---|---|
| Supabase | Postgres, 인증, Row Level Security, JSONB 저장이 쉬움 | 프로젝트 설정 필요 | 추천 |
| Firebase | 빠른 실시간 저장, 인증 쉬움 | 문서 구조와 권한 규칙 설계 필요 | 가능 |
| 자체 서버 + DB | 완전 자유 | 백엔드 운영 부담 큼 | 후순위 |
| 파일 저장만 | 단순 | 웹에서 배우별 자동 불러오기 어려움 | 백업용 유지 |

초기 웹 DB는 Supabase가 가장 적합하다.

추천 이유:

- `state`를 JSONB로 저장 가능
- `user_id` 기반 Row Level Security 적용 가능
- 배우 목록 쿼리가 단순함
- 나중에 정규화가 필요하면 Postgres 테이블로 확장 가능

### 31.6 Supabase 테이블 초안

#### `budget_states`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | uuid | 예산안 ID |
| `user_id` | uuid | 소유 사용자 |
| `actor_id` | uuid nullable | 배우 ID. 초기에는 null 가능 |
| `title` | text | 목록 표시명 |
| `actor_name` | text | 검색/목록용 중복 필드 |
| `company_name` | text | 검색/목록용 중복 필드 |
| `state` | jsonb | 현재 앱 state 전체 |
| `schema_version` | integer | state version |
| `created_at` | timestamptz | 생성일 |
| `updated_at` | timestamptz | 수정일 |

초기에는 `actors` 테이블을 만들지 않아도 된다.  
배우별 목록이 많아지면 다음 단계에서 분리한다.

#### 향후 `actors`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | uuid | 배우 ID |
| `user_id` | uuid | 소유 사용자 |
| `name` | text | 배우명 |
| `memo` | text | 메모 |
| `created_at` | timestamptz | 생성일 |
| `updated_at` | timestamptz | 수정일 |

### 31.7 권한 원칙

웹 DB를 붙이면 권한이 최우선이다.

규칙:

- 로그인한 사용자만 자신의 `budget_states`를 읽고 쓸 수 있다.
- `user_id = auth.uid()` 조건을 Row Level Security에 둔다.
- 공유 기능은 V1 웹 DB 범위에서 제외한다.

Supabase RLS 개념:

```sql
create policy "read own budget states"
on budget_states for select
using (auth.uid() = user_id);

create policy "insert own budget states"
on budget_states for insert
with check (auth.uid() = user_id);

create policy "update own budget states"
on budget_states for update
using (auth.uid() = user_id)
with check (auth.uid() = user_id);
```

### 31.8 저장 흐름

로컬:

```
state 변경
↓
debounce
↓
localStorage 저장
```

웹 DB:

```
state 변경
↓
localStorage 임시 저장
↓
debounce
↓
remote 저장
↓
updated_at 반영
```

네트워크 실패 시:

- 화면에 “로컬에 임시 저장됨” 표시
- 다음 저장 시 재시도
- JSON 내보내기는 항상 가능

### 31.9 로드 흐름

웹 DB:

```
로그인
↓
budget_states 목록 조회
↓
사용자 선택 또는 최근 항목 자동 선택
↓
state JSON 로드
↓
App.Defaults.ensureState(state)
↓
runSimulation(state)
```

중요:

- DB에서 읽은 state도 반드시 `ensureState`를 거친다.
- 구 JSON과 같은 마이그레이션 경로를 공유한다.
- DB 저장본이라고 해서 별도 마이그레이션을 만들지 않는다.

### 31.10 구현 순서

#### Phase DB-1 — 로컬 다중 예산안 준비

- `state.meta` 추가
- localStorage 저장 래퍼를 단일 state 저장에서 item 저장 구조로 확장
- 현재 UI는 `local-default` 하나만 사용
- 기존 localStorage 키에서 자동 마이그레이션

완료 기준:

- 기존 저장 데이터가 그대로 열린다.
- JSON export/import 결과가 변하지 않는다.
- `runSimulation` 결과가 변하지 않는다.

#### Phase DB-2 — 배우/예산안 목록 UI

- 간단한 목록 버튼 또는 상단 드롭다운
- 현재 예산안 이름 표시
- 새 예산안 만들기
- 이름 변경
- 삭제는 확인창 필수

현재 배우가 1명이므로 이 단계는 나중에 해도 된다.

#### Phase DB-3 — Supabase 연결

- 환경값: Supabase URL, anon key
- 로그인
- `budget_states` CRUD
- localStorage fallback 유지

완료 기준:

- 로그인 후 저장한 예산안이 다른 브라우저에서도 열린다.
- 로그아웃하면 로컬 모드 또는 읽기 불가 상태로 전환된다.
- 권한 없는 데이터는 조회되지 않는다.

Supabase 테이블 초안:

```sql
create table budget_states (
  id uuid primary key,
  user_id uuid not null references auth.users(id) on delete cascade,
  actor_id uuid,
  title text not null default '1인 기획사 예산안',
  actor_name text,
  company_name text,
  schema_version integer not null default 1,
  state jsonb not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

alter table budget_states enable row level security;

create policy "budget_states_select_own"
on budget_states for select
using (auth.uid() = user_id);

create policy "budget_states_insert_own"
on budget_states for insert
with check (auth.uid() = user_id);

create policy "budget_states_update_own"
on budget_states for update
using (auth.uid() = user_id)
with check (auth.uid() = user_id);

create policy "budget_states_delete_own"
on budget_states for delete
using (auth.uid() = user_id);
```

앱 연결 방식:

- `js/supabase-config.js`에 `enabled`, `url`, `anonKey`를 설정한다.
- Supabase가 꺼져 있거나 로그인 사용자가 없으면 기존 localStorage만 사용한다.
- 원격 저장본도 `state` JSONB 전체를 저장하고, 로드 후 반드시 `App.Defaults.ensureState(state)`를 통과한다.

#### Phase DB-4 — 동기화 안정화

- 저장 중 / 저장 완료 / 저장 실패 상태 표시
- 충돌 처리: 최신 `updated_at` 기준
- 수동 “서버에서 다시 불러오기”
- JSON 백업 유지

### 31.11 지금 바로 웹 DB를 붙일지 판단

지금 바로 Supabase를 붙일 수는 있다.  
하지만 아래 결정이 먼저 필요하다.

| 질문 | 결정 필요 |
|---|---|
| 로그인 방식 | 이메일? 구글? 비밀번호 없는 링크? |
| 데이터 소유자 | 사용자 1명만? 여러 사용자? |
| 배포 위치 | 정적 호스팅? 자체 도메인? |
| 공유 기능 | 없음? 읽기 공유? 편집 공유? |
| 비용 | 무료 tier로 충분한가? |

이 결정이 없으면 DB 코드는 붙어도 실제 사용 흐름이 흔들린다.

권장:

1. 먼저 Phase DB-1로 state 저장 구조만 배우/예산안 단위로 준비한다.
2. 이후 Supabase 프로젝트를 정하면 Phase DB-3을 붙인다.

### 31.12 하지 말 것

- 배우별로 `projects[]`, `employees[]`, `expenses[]`를 다른 DB 테이블에 먼저 쪼개지 않는다.
- 서버 DB가 생겼다고 JSON export/import를 제거하지 않는다.
- DB 저장본과 로컬 저장본의 마이그레이션 로직을 따로 만들지 않는다.
- `runSimulation`이 DB나 로그인 상태를 알게 만들지 않는다.
- 여러 배우 UI를 만들기 전까지 계산 엔진에 다중 배우 개념을 넣지 않는다.

### 31.13 완료 기준

배우별 저장 구조 완료 기준:

1. 배우 또는 예산안 단위로 state를 저장할 수 있다.
2. 선택한 actor/budget의 state만 로드된다.
3. 기존 단일 localStorage 저장본이 자동 마이그레이션된다.
4. 기존 JSON 파일을 그대로 가져올 수 있다.
5. `runSimulation` 결과는 저장 방식과 무관하게 동일하다.
6. 웹 DB를 붙여도 동일한 `ensureState` 경로를 탄다.
7. 권한 없는 사용자는 다른 사용자의 budget state를 읽거나 쓸 수 없다.

---

## 31.14 확정: 로그인 없는 로컬 다중 예산안 (지금 구현할 범위)

31.11의 열린 질문 중 "로그인 방식"에 대한 결정: **지금 단계에서는 로그인을 붙이지 않는다.**
사용자 1명, 배우도 아직 1명이라 로그인은 과하다. Phase DB-1(로컬 구조 준비)과 Phase DB-2(목록 UI)를
합쳐서 지금 바로 구현하고, Phase DB-3/DB-4(Supabase, 로그인)는 31장 본문 그대로 유보한다.
`js/remote-store.js` / `js/supabase-config.js`는 이미 Phase DB-3용으로 만들어져 있고 `enabled: false`라
지금 구조와 충돌하지 않는다 — 손대지 않는다.

### 31.14.1 기능 범위

1. 여러 예산안 저장
2. 저장 목록에서 불러오기
3. 새 예산안 만들기 (빈 상태 또는 현재 상태 복제)
4. 이름 변경
5. 복제
6. 삭제 (마지막 하나는 삭제 못 함)
7. JSON 내보내기/가져오기 (기존 기능 그대로 유지 — 백업·이동용)

저장 위치는 그대로 **사용자 브라우저 localStorage**다. 같은 PC·같은 브라우저에서는 계속 남지만
다른 기기·다른 브라우저에는 자동으로 안 보인다 — 그 경우 JSON 내보내기/가져오기로 옮긴다.

### 31.14.2 localStorage 구조 (index / item 분리)

예산안 하나를 저장할 때마다 전체 목록을 통째로 다시 쓰지 않도록, **index와 item을 분리한다.**
(31.6의 Supabase `budget_states` 테이블 구조와도 같은 모양이라 나중에 DB-3로 넘어갈 때 매핑이 쉽다.)

```javascript
"solo-agency-budget:index:v1" = {
  activeBudgetId: "b_xxx",
  items: [
    {
      id: "b_xxx",
      name: "이종원 예산안",
      actorName: "이종원",
      companyName: "",
      startMonth: "2026-11",
      endMonth: "2027-09",
      createdAt: "2026-08-20T00:00:00.000Z",
      updatedAt: "2026-08-20T00:10:00.000Z"
    },
    { id: "b_yyy", ... }
  ]
}

"solo-agency-budget:item:b_xxx" = { /* 기존 state 그대로, version/profile/projects/... */ }
"solo-agency-budget:item:b_yyy" = { /* ... */ }
```

- `listBudgets()`는 `index:v1`만 읽는다 — item 키를 하나도 안 건드린다.
- `save(state)`(활성 예산안 저장)는 **`item:{activeBudgetId}` 키 하나만** 다시 쓰고,
  index에서는 해당 항목의 `updatedAt`(+ 이름 판단에 쓰는 `actorName`/`companyName`/기간)만 갱신한다.
  다른 예산안의 item 키는 전혀 건드리지 않는다 — 예산안이 몇 개든 저장 비용은 O(1)이다.
- index 자체는 항목 메타데이터만 담아 항상 작아서, index 쓰기 실패와 item 쓰기 실패를 구분해
  처리할 수 있다 (item만 실패해도 index가 깨지지 않는다).

기존 `"solo-agency-budget:v2"`(단일 state) 키는 **지우지 않는다.** 새 구조로 넘어간 뒤에도 롤백
안전장치로 남겨 두고, 다음 저장부터는 새 키에만 쓴다.

### 31.14.3 마이그레이션 (최초 1회)

`App.Store.load()` 최초 호출 시:

1. `index:v1`이 이미 있으면 → 그대로 사용, 끝.
2. 없고 `solo-agency-budget:v2`(구 단일 state)가 있으면 → `id: "b_" + uid()`를 발급해
   `item:{id}`에 그 state를 그대로 옮기고, `index:v1`에 항목 1개
   (`name: state.profile.companyName || state.profile.actorName || "기본 예산안"`)를 만들어
   active로 지정한다. 구 키는 그대로 둔다.
3. 아무것도 없으면 → 빈 예산안 1개(`emptyState()` 또는 샘플 시드, 기존 `seedState()` 로직 그대로)를
   `item:{id}`에 넣고 `index:v1`에 항목을 만들어 active로 지정한다.

어느 경로든 마지막에 `App.Defaults.ensureState(state)`를 통과시킨다 — 기존 JSON import와 같은
마이그레이션 경로를 공유한다 (31.9의 원칙 그대로).

### 31.14.4 `App.Store` API 확장

기존 이름은 그대로 두고 동작만 "활성 예산안" 기준으로 바꾼다 (app.js의 `scheduleSave`/`boot` 호출부는
거의 손대지 않아도 됨). 새 메서드를 추가한다.

```javascript
App.Store = {
  // 기존 이름 유지, 내부만 "활성 예산안" item 하나를 읽고 쓰도록 변경
  save(state),          // item:{activeBudgetId} 갱신 + index의 해당 항목 메타만 갱신
  load(),                // 마이그레이션 처리 후 item:{activeBudgetId} 반환
  exportJson(state),     // 변경 없음
  parseImport(text),     // 변경 없음
  download(state),       // 변경 없음

  // 신규
  listBudgets(),                    // index.items 그대로 반환 — item 키를 읽지 않는다
  getActiveBudgetId(),
  createBudget(name, opts),         // opts.fromState 있으면 복제, 없으면 새 emptyState(). id 반환
  renameBudget(id, name),           // index 항목의 name만 갱신 (item은 안 건드림)
  duplicateBudget(id, newName),     // item:{id}를 읽어 item:{newId}로 복사 + index에 항목 추가
  deleteBudget(id),                 // item:{id} 키 삭제 + index에서 항목 제거. 마지막 1개면 거부
  switchActiveBudget(id)            // index.activeBudgetId 변경 + item:{id} 반환(ensureState 통과)
}
```

`listBudgets()`가 반환하는 각 항목은 31.14.2의 index 항목 그대로다 — 목록을 보여주려고 모든
예산안의 item을 읽거나 `runSimulation`을 돌리지 않는다.

### 31.14.5 저장 용량 / 실패 처리

- localStorage는 origin당 보통 5~10MB. 예산안 1개 state는 대체로 수십 KB 수준이라 수백 개도
  여유 있지만, UX상 30개를 넘으면 안내 문구를 띄운다 ("오래된 예산안은 JSON으로 내보내고 정리하세요").
- `localStorage.setItem`이 quota 초과로 던지면 (`save`/`createBudget`/`duplicateBudget` 전부) 기존
  `App.Store.save`처럼 `false`/`{ok:false}`를 반환하고 화면에 "저장 실패 — 용량 초과, JSON으로
  내보내 보관하세요" 배너를 띄운다. 예외를 던져서 화면을 하얗게 만들지 않는다.

### 31.14.6 화면 설계

상단 헤더, 기존 `JSON 내보내기`/`가져오기`/`초기화` 버튼 옆에 예산안 전환 버튼을 둔다.

```
[ 예산안: 이종원 예산안 ▾ ]   JSON 내보내기   JSON 가져오기   초기화
```

클릭하면 목록 패널이 열린다.

```
예산안 목록
──────────────────────────────
● 이종원 예산안        26-11~27-09   기말 15억      [불러오기] [이름변경] [복제] [삭제]
  하렘 없는 시나리오    27-01~27-12   기말 8억       [불러오기] [이름변경] [복제] [삭제]
──────────────────────────────
[+ 새 예산안 (빈 상태)]   [+ 새 예산안 (현재 복제)]
```

- `●`는 현재 활성 예산안 표시.
- 삭제는 확인창 필수, 마지막 1개는 삭제 버튼 비활성화.
- 불러오기는 활성 예산안을 바꾸고 `recompute → refreshSticky → renderMain`을 그대로 호출한다
  (기존 JSON import 처리 흐름과 동일한 경로 재사용, 새 렌더 경로를 만들지 않는다).

### 31.14.7 app.js 연결

- `boot()`: `App.Store.load()`가 마이그레이션까지 처리하므로 호출부는 거의 그대로.
- `scheduleSave()`: `App.Store.save(state)` 호출 그대로 유지 (내부 동작만 바뀜).
- 신규 액션: `open-budget-list`, `switch-budget`, `new-budget`, `duplicate-budget`,
  `rename-budget`, `delete-budget`.

### 31.14.8 테스트 계획

- 구 단일 키만 있는 상태 → 로드 시 예산안 1개로 자동 승격, `runSimulation` KPI 불변.
- 아무 키도 없는 최초 실행 → 시드 예산안 1개 생성.
- `createBudget`/`renameBudget`/`duplicateBudget`/`deleteBudget`/`switchActiveBudget` 각각 정상 동작.
- 마지막 예산안 삭제 시도 → 거부.
- 복제한 예산안은 원본과 완전히 독립 (한쪽 수정해도 다른 쪽 불변).
- `listBudgets()`가 무거운 계산 없이 요약만 반환.
- localStorage quota 실패를 흉내내도 예외 없이 `false`/`{ok:false}` 반환.
- 기존 JSON export/import는 예산안 구조와 무관하게 그대로 동작.
- 기존 테스트 스위트(`node test/node-run.js` 기준 지금 통과하는 전체 개수) 전부 그대로 통과 + 신규 테스트 추가.

### 31.14.9 하지 말 것

- `js/remote-store.js` / `js/supabase-config.js`를 지금 단계에서 건드리거나 활성화하지 않는다.
- 예산안별로 `projects[]` / `employees[]` 등을 분리 저장하지 않는다 (31.12 원칙 그대로).
- 예산안 전환 로직을 `runSimulation` 안에 넣지 않는다 — 전환은 저장 계층에서만 일어난다.
- 기존 `App.Store.exportJson` / `parseImport` / `download`의 시그니처를 바꾸지 않는다.

### 31.14.10 완료 기준

1. 여러 예산안을 만들고, 목록에서 골라 불러올 수 있다.
2. 이름 변경·복제·삭제가 모두 되고, 마지막 하나는 삭제되지 않는다.
3. 기존 단일 저장본이 새로고침 한 번으로 자동 마이그레이션된다 (데이터 손실 없음).
4. JSON 내보내기/가져오기가 예산안 구조 도입 전과 동일하게 동작한다.
5. Supabase/RemoteStore 코드는 변경되지 않은 채 그대로 유보 상태를 유지한다.
6. `node test/node-run.js` 기준 기존 테스트 전부 + 신규 테스트 포함 FAIL 0.

---

## 32. 세금 귀속연도 분리 (지금 구현할 범위)

- 작성일: 2026-08-21
- 상태: 구현 착수 전 설계. 이 장이 착수 범위다.
- 전제: 계산 엔진의 **세율 숫자 자체**는 바꾸지 않는다. 바꾸는 것은 **어느 기간의 소득에 그 세율을 적용하는가**다.
- 이 장은 §17의 `과세표준 추정 = 기간 매출 − 기간 pnlExpense` (시뮬레이션 전체를 한 과세기간으로 보는 식)를 **폐기**하고 대체한다.

### 32.1 한 줄

시뮬레이션 기간과 과세기간은 다른 개념이다.

월별 손익을 귀속연도로 나눈 뒤, 연도마다 법인세·종합소득세·근로소득세를 계산하고, 화면 합계만 다시 더한다.

예: `2026-10 ~ 2027-12` → `2026-10~12`와 `2027-01~12`를 각각 계산한다. 15개월 합산액에 세율을 한 번 적용하지 않는다.

### 32.2 왜 지금 하는가

누진세는 과세연도 단위다. 15개월을 한 과세표준으로 넣으면:

- 2026 소득과 2027 소득이 한 구간에 합쳐져 최고세율이 과대 적용된다.
- 2026 설립 초기 적자가 2027 이익을 깎는다 (실제 법인세는 그렇게 상쇄하지 않는다. 결손금 이월은 별도 제도).
- 대표 월급 2,000만 × 15개월 = 3억을 **한 해 총급여**로 보면 근로소득공제·세액공제가 왜곡된다.

기간을 2028까지 늘릴수록 왜곡이 커진다. 연도 버킷만 맞으면 기간을 늘려도 엔진은 그대로다.

### 32.3 이번 범위 / 다음 작업

이번 작업은 **세금 귀속연도 분리 + 그 결과를 화면에 보여주는 것**만 한다.

| 포함 | 제외 (다음 작업) |
|---|---|
| 월별 손익 → 귀속연도 버킷 | 국민연금 상·하한, 직원별 보험료 |
| 법인세 연도별 계산 후 합산 | 건강보험 보수월액 상한, 장기요양 |
| 전속 종합소득세 연도별 계산 후 합산 | 퇴직급여를 근무월/12로 강제 변경 |
| 1인 기획사 근로소득세는 기존 연도분리 유지, 화면에 연도 표시 | 복리후생 `(식대+50%)×2` → `식대×2` |
| 한눈에 비교 / 세금 상세에 2026·2027 분리 | 법인세 결손금 이월 |
| 기존 KPI 합계 필드는 파생값으로 유지 | 연도별 세금 현금 납부월 여러 개 |
| | ledger source id 체계 재설계 |

제외 항목은 원장·현금·세전이익을 같이 바꾼다. 세금 연도분리와 한 작업에 넣으면 검산이 불가능하다.

이미 되어 있어서 **다시 만들지 않는 것:**

- 1인 기획사 대표 급여 근로소득세: `ownerSalaryByYear` → 연도별 `calculatePersonalTaxDetail` → `aggregateTaxDetails`. 테스트 `대표 급여 연도별 근로소득세`가 6,000만(2026)+2.4억(2027) 합산을 이미 검증한다.
- 식대는 비용이 아님. 원장에는 복리후생비 하나만 들어간다.
- 산재요율은 `settings.insuranceRates.industrialAccident`. 엔진 하드코딩 아님.
- 퇴직 대상은 `emp.severance`. 대표 기본은 `false`.
- 대표 급여는 법인 판관비(비용) + 개인 근로소득. 개인 쪽에서 다시 비용 처리하지 않는다. 이 구조를 건드리지 않는다.

### 32.4 현재 코드에서 깨진 경로

조사 결과. 구현 전에 이 경로만 고친다.

| 계산 | 파일 | 현재 | 목표 |
|---|---|---|---|
| 법인세 | `js/engine/index.js` → `calculateEstimatedTax(profit)` | `calculateProfit`이 전 기간 매출·비용을 합친 뒤 세율 1회 | 연도별 세전이익에 각각 세율 |
| 법인세 현금 | `js/engine/cashflow.js` `applyTaxCashOut` | `cashOutMonth` 하나에 합계 세액 | 이번엔 합계 세액을 기존처럼 한 달에 넣는다. 납부월을 연도별로 쪼개지 않음 |
| 전속 종소세 | `js/engine/scenarios.js` `calculateExclusiveContractScenario` | `(기간매출 − 배전공제) × 배분율`을 한 과세표준 | 연도별 매출·배전공제·배분율 → 연도별 종소세 → 합산 |
| 화면 귀속연도 | `settings.personalTaxCommon.year` | auto 모드에서도 연도 1개가 두 시나리오 세율표를 강제 | auto 모드는 시뮬레이션이 걸친 모든 과세연도를 쓴다. 드롭다운으로 계산을 제한하지 않음 |
| 1인 기획사 종소세 | `calculateSoloPersonalTaxDetail` | 연도별 계산됨. 화면은 합계만 표시 | 계산 유지. 세금 상세에 연도 표시 |

§17 식 `과세표준 = max(0, 기간 매출 − 기간 pnlExpense)`는 이제 **한 과세연도 안에서만** 성립한다.

### 32.5 공통 Year Bucket

세금 계산 전에 월별 흐름을 연도로 묶는 helper를 한 곳에 둔다. 법인세와 전속 종소세가 각자 연도를 자르지 않는다.

```javascript
App.TaxYear.yearsOf(months)           // ["2026","2027"] 정렬
App.TaxYear.yearOf(month)             // "2026-10" → 2026
App.TaxYear.groupByYear(months, pick) // { 2026: { months, ...pick }, 2027: ... }
```

위치 제안: `js/engine/tax-year.js` (신규). `js/engine/tax.js`에  squatting하지 않는다. tax.js는 세액 계산만 한다.

버킷에 넣을 월별 값:

- `inflow` (기간 입금. `otherInflow` 제외 — 현행 법인세 과세표준과 동일)
- `pnlExpense` (현행과 동일. `taxCashOut` 제외)
- 전속 배분 전 공제에 해당하는 월별 비용 (프로젝트 직접비, 매출연동 수수료 등 `costBurdenRules`가 `deductBeforeSplit`인 항목)

### 32.6 법인세

```
for year in taxYears:
  매출[year]     = Σ inflow of year
  손익비용[year] = Σ pnlExpense of year
  세전이익[year] = 매출[year] − 손익비용[year]
  과세표준[year] = max(0, 세전이익[year])
  법인세[year]   = pickBracket(과세표준[year], getCorporateTaxRule(year))
  지방세[year]   = round(법인세[year] * localTaxRate(year))
  합계[year]     = 법인세[year] + 지방세[year]

kpis.tax          = Σ 합계[year]          // 파생. 필드명은 유지
kpis.taxDetail    = { byYear, corporate: Σ, local: Σ, total: Σ, taxable: Σ 과세표준 }
kpis.operatingProfit = Σ 세전이익[year]   // 기간 합. 지금과 같은 숫자
kpis.profitAfterTax  = operatingProfit − kpis.tax
```

금지:

- 전체기간 세전이익에 세율을 한 번 적용
- 2026 적자로 2027 과세표준을 깎기 (결손금 이월은 다음 작업)
- `taxCashOut`을 과세표준에 넣기 (§17과 동일)

세율표:

- 신규 `js/data/corporate-tax.js`. `getCorporateTaxRule(year)` → `{ brackets, localRate, source }`.
- 2026·2027 기본 구간은 지금 `settings.tax.corporateBrackets`와 동일하게 둔다 (세율 숫자를 바꾸지 않는다).
- `settings.tax.corporateBrackets` / `localTaxRate`는 **사용자 덮어쓰기**로 남긴다. 있으면 그 해 기본표보다 우선. 없으면 연도 테이블.
- 함수 본문에 `0.09` 같은 숫자를 새로 박지 않는다.

한 과세연도만 걸친 시뮬레이션은 **지금과 세액이 같아야** 한다.

`applyTaxCashOut`: `cashOutMonth`가 있으면 **합계 세액**을 그 달에 넣는다. 연도별 납부월은 이번에 만들지 않는다. 기본값은 계속 `null` (현금 미차감).

### 32.7 전속 종합소득세

전속 배우 귀속소득은 사업소득이다. 전체 15개월을 한 과세표준으로 넣지 않는다.

```
for year in taxYears:
  매출[year]       = Σ 그 해 입금
  배전공제[year]   = Σ 그 해 deductBeforeSplit 비용
  배분기준[year]   = max(0, 매출[year] − 배전공제[year])
  귀속소득[year]   = round(배분기준[year] * actorShareRate)
  배우부담[year]   = Σ 그 해 배우 부담 비용 + 그 해 배우 부담 지원
  과세소득[year]   = max(0, 귀속소득[year] − 배우부담[year] ± 과세조정)
  종소세[year]     = calculatePersonalTaxDetail(과세소득[year], { year, incomeType: "business", ... })

personalTax        = Σ 종소세[year].totalPersonalTax
actorGrossIncome   = Σ 귀속소득[year]
actorNetIncome     = actorGrossIncome − 기간 배우부담비용 − personalTax
```

핵심: **매출만 연도로 나누고 배전공제를 15개월 통으로 빼면 안 된다.** 2026 소득이 과소, 2027이 과대가 된다. 비용도 발생월로 같은 버킷에 넣는다.

`mode === "manual" | "rate"`: 사용자가 넣은 기간 세액/세율을 그대로 쓴다. 연도 분할하지 않음.

`personalTaxDetail` 모양은 1인 기획사와 맞춘다.

```
personalTaxDetail = aggregateTaxDetails(yearDetails, settings)
personalTaxDetail.years = [ { year, ...detail }, ... ]
```

기존 `aggregateTaxDetails`를 전속도 재사용한다. 합산 키에 `taxableBase` 같은 참고값을 더하는 것은 표시용이다. 세액은 각 연도 `totalPersonalTax`의 합이 SSOT다.

### 32.8 1인 기획사 개인세금

계산 경로를 바꾸지 않는다. `calculateSoloPersonalTaxDetail`이 이미 월별 대표급여를 연도로 나눈다.

이번 작업에서 할 일:

- `personalTaxDetail.years`를 화면이 읽게 한다.
- auto 모드에서 `personalTaxCommon.year`가 연도별 `yearSettings.year`를 덮어쓰지 않는지 확인한다. (지금은 버킷 연도가 이긴다. 유지.)
- 화면에서 「대표 급여 3억 = 한 해 총급여」처럼 보이지 않게, 연도별 총급여를 세금 상세에 적는다.

금지:

- 15개월 급여 합계를 `calculateEarnedIncomeDeduction`에 한 번 넣기
- 급여를 법인 판관비에서 빼기

### 32.9 결과 데이터

연도별이 SSOT. 총액은 저장하지 않고 합산한다. 다만 기존 필드명은 유지한다. 엑셀·KPI·테스트가 `kpis.tax`, `corporateTax`, `personalTax`를 읽는다.

```javascript
// runSimulation 결과
kpis.taxDetail = {
  byYear: {
    2026: { revenue, pnlExpense, taxableIncome, corporateTax, localIncomeTax, totalTax },
    2027: { ... }
  },
  taxable: Σ taxableIncome,     // 참고. 이 합에 세율을 다시 적용하지 않음
  corporate: Σ corporateTax,
  local: Σ localIncomeTax,
  total: Σ totalTax
}

// 시나리오 행
scenario.personalTaxDetail.years = [ ... ]
scenario.corporateTaxByYear = { 2026: n, 2027: n }   // 1인 기획사만. 전속은 없음
scenario.personalTaxByYear = { 2026: n, 2027: n }
```

법인세를 개인세금 필드에 넣지 않는다. (`totalTaxBurden`은 지금처럼 법인+개인 참고값. 배우용 요약은 분리 표시.)

### 32.10 화면

배우용 결론 카드(실수령 / 법인 잔여 / 경제가치 차이)는 유지한다. 첫 화면에서 세법 용어를 늘리지 않는다.

세금 관련 숫자는 이렇게 나눈다.

**요약 (기본)**

```
기존 회사 전속    개인 세금 총액
1인 기획사        개인 세금 총액
                  법인 세금 총액
```

**세금 계산 상세 보기 ▾** (기본 접힘)

```
2026 귀속
  법인세
  대표 근로소득세 (1인 기획사)
  기존회사 종합소득세
  각 지방소득세

2027 귀속
  (동일 구조)

────────────────
시뮬레이션 전체기간 합계
  개인 세금 총액
  법인 세금 총액
```

기간이 2028을 넘으면 연도 블록이 늘어난다. 연도 UI를 하드코딩하지 않는다.

auto 모드:

- `귀속연도 [2026 ▼]` 한 개로 계산을 제한하는 컨트롤은 비교 화면에서 제거하거나, “세율표는 각 귀속연도 기준” 안내로 바꾼다.
- `계산 조건 및 세부 설정`의 수동 세액·유효세율·기타소득은 지금처럼 접힘 안에 둔다.

상세 계산표(근로소득공제, 과세표준, 누진공제 등)는 **선택한 연도** 또는 연도별 소표로 보여 준다. 15개월을 한 표의 과세표준으로 다시 합쳐 보여 주지 않는다.

분석 > 월별 분석의 「수익 / 세금 분석」 미니표는 `kpis.tax` 합계를 쓰되, 연도가 2개 이상이면 연도별 한 줄을 보탠다.

### 32.11 파일별 손댈 곳

| 파일 | 할 일 |
|---|---|
| `js/engine/tax-year.js` | **신규.** year bucket helper |
| `js/data/corporate-tax.js` | **신규.** `getCorporateTaxRule(year)` |
| `js/engine/tax.js` | 연도별 법인세. `calculateEstimatedTax`는 한 해 입력용으로 유지하고, 기간 합산 진입점을 없앤다 |
| `js/engine/index.js` | profit/tax를 연도 버킷으로 연결. `kpis.taxDetail.byYear` |
| `js/engine/scenarios.js` | 전속 종소세 연도 분할. 비교 ViewModel에 `*ByYear` |
| `js/engine/personal-tax.js` | 세액 함수 시그니처 유지. 기간 합산을 여기서 하지 않음 |
| `js/data/personal-tax.js` | 건드리지 않음 (세율 숫자 유지) |
| `js/render.js` | 한눈에 비교 세금 상세 연도 블록. auto 연도 드롭다운 정리 |
| `js/export-excel.js` | 요약 시트 법인세가 합계임을 명시. 가능하면 연도 행 추가 |
| `test/cases.js` | 32.13 |
| `js/engine/payroll.js` `defaults.js` 식대/퇴직 | **금지.** 이번 범위 아님 |

### 32.12 하지 말 것

- 전체 시뮬레이션 세전이익 × 법인세율
- 전속 15개월 개인소득 × 종합소득세율
- 15개월 대표급여를 한 해 총급여로 과세 (이미 금지됨. 회귀만)
- 국민연금·건강보험 상한, 직원별 보험 재계산
- 복리후생 산식 변경
- 퇴직급여 모드 변경 (`decemberFull` 유지)
- 결손금 이월
- 식대 비용을 원장에 추가
- 대표 급여를 법인 비용에서 제거
- ledger id 체계 재작성
- `kpis.tax` / `corporateTax` / `personalTax` 필드 삭제
- 배우용 결론 카드의 정보구조를 다시 세무 계산표로 되돌리기

### 32.13 테스트

기존 하드코딩 세액(예: 전속 `personalTax === 251526000`)은 **연도분리 후 숫자가 바뀌는 것이 정상**이다. 그 테스트는 “한 과세표준 값”이 아니라 “연도별 합 = 엔진 결과”로 고친다.

한 과세연도만 걸친 픽스처는 현재 세액과 같아야 한다.

필수:

| ID | 내용 |
|---|---|
| T1 | 기간이 한 과세연도 안 → 법인세·전속 종소세가 현재 공식과 동일 |
| T2 | `2026-10~2027-12` → 법인세 = 2026세액 + 2027세액. 전체 세전이익에 세율 1회 적용한 값과 **다름** |
| T3 | 3개 과세연도 → `taxDetail.byYear` 키 3개. 엔진 분기 없이 동일 코드 |
| T4 | 전속 종소세 ≠ 기간 총 귀속소득을 한 번 과세한 값 |
| T5 | 대표 15개월 2,000만 → 근로소득세 = 6,000만 과세 + 2.4억 과세 (기존 테스트 유지) |
| T6 | 1인 기획사 `personalTaxDetail.years`에 2026·2027이 있고, 합이 `personalTax` |
| T7 | 전속도 `personalTaxDetail.years` 동일 구조 |
| T8 | `operatingProfit`·원장 금액·보험·복리후생·퇴직은 이 작업으로 불변 (세금 현금차감 없을 때 기말현금도 불변) |
| T9 | 화면 HTML에 `2026 귀속` / `2027 귀속` / 전체기간 합계 문구. 과세표준 합계로 세율을 다시 적용한 숫자가 없음 |
| T10 | `node test/node-run.js` FAIL 0 |

T8이 깨지면 범위 밖 산식을 만진 것이다.

### 32.14 구현 순서

1. `tax-year.js` helper + 단위 테스트 (월 목록 → 연도)
2. `corporate-tax.js` 테이블. 기본 구간은 현행과 동일
3. `runSimulation` 법인세 연도별. T1·T2·T8
4. 전속 종소세 연도별. T4·T7
5. 비교 ViewModel `*ByYear` 연결. 1인 기획사 years 노출
6. 한눈에 비교 UI 연도 블록. T9
7. 엑셀 요약에 연도 행 (있으면)
8. 기존 하드코딩 세액 테스트 수정 + T3·T5·T6·T10

한 단계마다 테스트를 그린으로 두고 다음으로 간다. 복리후생·보험·퇴직 파일을 이 순서에 끼워 넣지 않는다.

### 32.15 완료 기준

1. `2026-10~2027-12`에서 법인세와 전속 종소세가 연도별 계산의 합이다.
2. 같은 데이터를 15개월 한 과세표준으로 넣은 값과 세액이 다르다.
3. 한 해만 있는 시뮬레이션은 세액이 작업 전과 같다.
4. 배우용 첫 화면은 총액, 세금 상세는 연도별, 맨 아래만 전체기간 합계다.
5. 원장·보험·복리후생·퇴직·기말현금(세금 미차감)은 불변이다.
6. `node test/node-run.js` FAIL 0.

---

## 33. 세금 고도화 Phase 2 — 이월결손금

- 작성일: 2026-08-21
- 전제: §32 귀속연도 버킷은 유지한다. 이번엔 **연도 독립 계산 위에 결손금 이월만** 얹는다.

### 33.1 범위

| 포함 | 제외 |
|---|---|
| 법인세 이월결손금 (다음 해로만, 소급 없음) | 국민연금 상한, 퇴직, 복리후생 |
| 시작 전 이월 잔액 | 개인 종합소득 결손금 |
| 세무조정 배열 (연도별 금액, 기본 빈 목록) | 접대비 한도 자동계산 |
| 연도별 세금 현금납부 옵션: 다음 해 3월 | 기본값을 현금 차감으로 바꾸기 |

기본값:

- `lossCarryforward.apply = true` (중소기업, 공제한도 100%. JSON에 `enabled` 키를 쓰지 않음)
- `cashOutMonth = null` (현금은 지금처럼 안 깎음)
- `cashOutMode = "none"`
- `adjustments = []`

한 해 적자를 같은 해 다른 달과 합치는 것(§32 이전)과, **다음 과세연도 과세표준에서 빼는 것**은 다른 제도다.

### 33.2 산식

```
carry = openingBalance
for year in taxYears:
  장부이익 = 매출[year] − 손익비용[year]
  세무조정 = Σ adjustments where year
  조정후이익 = 장부이익 + 세무조정

  if not enabled:
    과세표준 = max(0, 조정후이익)
  else if 조정후이익 <= 0:
    과세표준 = 0
    carry += −조정후이익
  else:
    한도 = 조정후이익 * limitRate
    사용 = min(carry, 한도)
    과세표준 = 조정후이익 − 사용
    carry -= 사용

  법인세[year] = pickBracket(과세표준, year)
```

금지: 2027 적자로 2026 세액을 깎기.

### 33.3 현금납부

- `cashOutMonth`가 있으면 합계 세액을 그 달에 넣는다.
- `cashOutMode === "nextMarch"` 이고 지정월이 없으면, 각 연도 세액을 `(year+1)-03`에 넣는다. 기간 밖이면 그 해 분은 현금 미차감.

### 33.4 하지 말 것

원장·보험·식대·퇴직 산식 변경. 기본 현금차감 켜기.

---

## 34. 인건비 고도화 — 보험 상한 · 퇴직 안분

- 작성일: 2026-08-21
- 전제: 세금 연도분리·이월결손금은 유지한다. 이번엔 **원장의 보험·퇴직**만 맞춘다.

### 34.1 범위

| 포함 | 제외 |
|---|---|
| 직원별 4대보험 계산 | 국민연금 요율 4.5%→4.75% 자동 변경 |
| 국민연금 기준소득월액 상·하한 (`yearMonth`) | 고용보험 보수월액 상한 |
| 건강보험 보수월액 상한 | 장기요양을 원장 별도 행으로 분리 |
| 퇴직급여 기본 = 월급여/12 재직월 안분 | 복리후생 `(식대+50%)×2` 변경 (회식 여유는 유지) |
| `decemberFull` / `manual` 옵션 유지 | ledger source id 재설계 |

기본값: `insuranceRates.useCaps = true`, `severance.mode = "auto"`.

### 34.2 산식

국민연금 기준(회사분) = clamp(그 직원 그달 급여, 하한, 상한) × pensionEmployer  
건강보험 기준 = min(그 직원 그달 급여, 건보 상한) × health  
고용·산재 = 그 직원 그달 급여 × 요율 (상한 없음)

퇴직(auto) 월액 = 그달 퇴직 대상 월급 합 / 12

금지: 전 직원 월급을 더한 뒤 한 번에 국민연금을 매기기. 대표 2,000만 × 4.5%.


